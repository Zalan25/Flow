/************************************************************/
/*****              SqlDataProvider                     *****/
/*****       DNN Module: QuizLearn                      *****/
/*****       Version: 00.00.10                          *****/
/*****                                                  *****/
/*****       Imports default level-test questions       *****/
/*****       for all supported languages.               *****/
/************************************************************/

/* =========================================================
   Source: english.sql -> language code en
   ========================================================= */
/* ---------------------------------------------
   English level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @EnglishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @EnglishId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'en';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* Helper pattern:
   insert question if not exists, then insert answers if missing.
*/


/* ---------------------------------------------
   A1 questions - 5 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct word: I ___ a student.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct word: I ___ a student.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'am', 1, 1),
    (@ModuleId, @Q1, N'is', 0, 2),
    (@ModuleId, @Q1, N'are', 0, 3),
    (@ModuleId, @Q1, N'be', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "Book" means "könyv".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "Book" means "könyv".', @EnglishId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'True', 1, 1),
    (@ModuleId, @Q2, N'False', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct plural: one child, two ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct plural: one child, two ___.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'childs', 0, 1),
    (@ModuleId, @Q3, N'children', 1, 2),
    (@ModuleId, @Q3, N'childes', 0, 3),
    (@ModuleId, @Q3, N'childrens', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct answer: What is the opposite of "big"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct answer: What is the opposite of "big"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'small', 1, 1),
    (@ModuleId, @Q4, N'tall', 0, 2),
    (@ModuleId, @Q4, N'fast', 0, 3),
    (@ModuleId, @Q4, N'new', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Ez egy ház."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Ez egy ház."', @EnglishId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'This is a house', 1, 1),
    (@ModuleId, @Q5, N'It is a house', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 5 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct form: She ___ to school every day.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct form: She ___ to school every day.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'go', 0, 1),
    (@ModuleId, @Q6, N'goes', 1, 2),
    (@ModuleId, @Q6, N'going', 0, 3),
    (@ModuleId, @Q6, N'gone', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all words that are fruits.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all words that are fruits.', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'apple', 1, 1),
    (@ModuleId, @Q7, N'banana', 1, 2),
    (@ModuleId, @Q7, N'table', 0, 3),
    (@ModuleId, @Q7, N'orange', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "Yesterday I go to the cinema" is grammatically correct.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "Yesterday I go to the cinema" is grammatically correct.', @EnglishId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'True', 0, 1),
    (@ModuleId, @Q8, N'False', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct sentence.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct sentence.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'I have got two brothers.', 1, 1),
    (@ModuleId, @Q9, N'I has got two brothers.', 0, 2),
    (@ModuleId, @Q9, N'I having two brothers.', 0, 3),
    (@ModuleId, @Q9, N'I am have two brothers.', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Tegnap filmet néztem."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Tegnap filmet néztem."', @EnglishId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'Yesterday I watched a film', 1, 1),
    (@ModuleId, @Q10, N'Yesterday I watched a movie', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 5 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct option: If I ___ more time, I would travel more.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct option: If I ___ more time, I would travel more.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'have', 0, 1),
    (@ModuleId, @Q11, N'had', 1, 2),
    (@ModuleId, @Q11, N'will have', 0, 3),
    (@ModuleId, @Q11, N'has', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all words related to work.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all words related to work.', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'employee', 1, 1),
    (@ModuleId, @Q12, N'salary', 1, 2),
    (@ModuleId, @Q12, N'holiday', 0, 3),
    (@ModuleId, @Q12, N'office', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "I have lived here for five years" means the speaker still lives here.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "I have lived here for five years" means the speaker still lives here.', @EnglishId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'True', 1, 1),
    (@ModuleId, @Q13, N'False', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Szerintem ez jó ötlet."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Szerintem ez jó ötlet."', @EnglishId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'I think this is a good idea', 1, 1),
    (@ModuleId, @Q14, N'In my opinion this is a good idea', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Write one sentence about your last holiday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Write one sentence about your last holiday.', @EnglishId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'I went', 1, 1),
    (@ModuleId, @Q15, N'I visited', 1, 2),
    (@ModuleId, @Q15, N'I stayed', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 3 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the best synonym for "significant".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the best synonym for "significant".', @EnglishId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'important', 1, 1),
    (@ModuleId, @Q16, N'ordinary', 0, 2),
    (@ModuleId, @Q16, N'tiny', 0, 3),
    (@ModuleId, @Q16, N'unclear', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all formal expressions.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all formal expressions.', @EnglishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'I would like to request...', 1, 1),
    (@ModuleId, @Q17, N'Could you please...', 1, 2),
    (@ModuleId, @Q17, N'Gonna do it', 0, 3),
    (@ModuleId, @Q17, N'Hey mate', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Annak ellenére, hogy fáradt voltam, befejeztem a munkát."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Annak ellenére, hogy fáradt voltam, befejeztem a munkát."', @EnglishId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'Although I was tired I finished the work', 1, 1),
    (@ModuleId, @Q18, N'Despite being tired I finished the work', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 2 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the best phrase: His argument was persuasive, albeit somewhat ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the best phrase: His argument was persuasive, albeit somewhat ___.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'flawed', 1, 1),
    (@ModuleId, @Q19, N'hungry', 0, 2),
    (@ModuleId, @Q19, N'wooden', 0, 3),
    (@ModuleId, @Q19, N'silent', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Write one complex sentence using the word "nevertheless".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Write one complex sentence using the word "nevertheless".', @EnglishId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'nevertheless', 1, 1);
END
GO
/* ---------------------------------------------
   English level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @EnglishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @EnglishId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'en';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* ---------------------------------------------
   A1 questions - 5 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct word: ___ name is Anna.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct word: ___ name is Anna.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'Her', 1, 1),
    (@ModuleId, @Q1, N'She', 0, 2),
    (@ModuleId, @Q1, N'Hers', 0, 3),
    (@ModuleId, @Q1, N'Him', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "Cat" means "kutya".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "Cat" means "kutya".', @EnglishId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'True', 0, 1),
    (@ModuleId, @Q2, N'False', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct number: five + three = ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct number: five + three = ___.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'seven', 0, 1),
    (@ModuleId, @Q3, N'eight', 1, 2),
    (@ModuleId, @Q3, N'nine', 0, 3),
    (@ModuleId, @Q3, N'six', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'What colour is the sky on a clear day?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'What colour is the sky on a clear day?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'blue', 1, 1),
    (@ModuleId, @Q4, N'green', 0, 2),
    (@ModuleId, @Q4, N'red', 0, 3),
    (@ModuleId, @Q4, N'yellow', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Jó reggelt!"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Jó reggelt!"', @EnglishId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'Good morning', 1, 1),
    (@ModuleId, @Q5, N'Good morning!', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 5 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct form: They ___ football yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct form: They ___ football yesterday.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'play', 0, 1),
    (@ModuleId, @Q6, N'played', 1, 2),
    (@ModuleId, @Q6, N'plays', 0, 3),
    (@ModuleId, @Q6, N'playing', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all words that describe weather.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all words that describe weather.', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'sunny', 1, 1),
    (@ModuleId, @Q7, N'rainy', 1, 2),
    (@ModuleId, @Q7, N'hungry', 0, 3),
    (@ModuleId, @Q7, N'windy', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "She don''t like coffee" is grammatically correct.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "She don''t like coffee" is grammatically correct.', @EnglishId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'True', 0, 1),
    (@ModuleId, @Q8, N'False', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct question: ___ do you live?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct question: ___ do you live?', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'Where', 1, 1),
    (@ModuleId, @Q9, N'What', 0, 2),
    (@ModuleId, @Q9, N'Who', 0, 3),
    (@ModuleId, @Q9, N'When', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Szeretek úszni."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Szeretek úszni."', @EnglishId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'I like swimming', 1, 1),
    (@ModuleId, @Q10, N'I love swimming', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 5 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct option: By the time she arrived, we ___ for an hour.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the correct option: By the time she arrived, we ___ for an hour.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'waited', 0, 1),
    (@ModuleId, @Q11, N'had been waiting', 1, 2),
    (@ModuleId, @Q11, N'have waited', 0, 3),
    (@ModuleId, @Q11, N'were waiting', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all words that can follow "make".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all words that can follow "make".', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'a decision', 1, 1),
    (@ModuleId, @Q12, N'a mistake', 1, 2),
    (@ModuleId, @Q12, N'a travel', 0, 3),
    (@ModuleId, @Q12, N'progress', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'True or false: "Used to" describes a habit that no longer happens.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'True or false: "Used to" describes a habit that no longer happens.', @EnglishId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'True', 1, 1),
    (@ModuleId, @Q13, N'False', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Régóta tanulok angolul."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Régóta tanulok angolul."', @EnglishId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'I have been learning English for a long time', 1, 1),
    (@ModuleId, @Q14, N'I have been studying English for a long time', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Write one sentence about what you would do if you won the lottery.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Write one sentence about what you would do if you won the lottery.', @EnglishId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'I would', 1, 1),
    (@ModuleId, @Q15, N'I could', 1, 2),
    (@ModuleId, @Q15, N'I might', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 3 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the best synonym for "ambiguous".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the best synonym for "ambiguous".', @EnglishId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'unclear', 1, 1),
    (@ModuleId, @Q16, N'certain', 0, 2),
    (@ModuleId, @Q16, N'simple', 0, 3),
    (@ModuleId, @Q16, N'cheerful', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Select all sentences that use the passive voice correctly.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Select all sentences that use the passive voice correctly.', @EnglishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'The report was written by Sarah.', 1, 1),
    (@ModuleId, @Q17, N'The cake has been eaten.', 1, 2),
    (@ModuleId, @Q17, N'They built the bridge.', 0, 3),
    (@ModuleId, @Q17, N'The window was broken by the ball.', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Translate into English: "Bárcsak több időm lenne tanulni."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Translate into English: "Bárcsak több időm lenne tanulni."', @EnglishId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'I wish I had more time to study', 1, 1),
    (@ModuleId, @Q18, N'If only I had more time to study', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 2 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the word that best completes the sentence: The new policy was met with ___ opposition from the public.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Choose the word that best completes the sentence: The new policy was met with ___ opposition from the public.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'vehement', 1, 1),
    (@ModuleId, @Q19, N'cheerful', 0, 2),
    (@ModuleId, @Q19, N'narrow', 0, 3),
    (@ModuleId, @Q19, N'silent', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'en');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Write one complex sentence using the word "notwithstanding".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Write one complex sentence using the word "notwithstanding".', @EnglishId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'notwithstanding', 1, 1);
END
GO
/* =========================================================
   Source: greek.sql -> language code el
   ========================================================= */
/* ---------------------------------------------
   Greek level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @GreekId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @GreekId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'el';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* ---------------------------------------------
   A1 questions - 10 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή λέξη: ___ είμαι μαθητής.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή λέξη: ___ είμαι μαθητής.', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'Εγώ', 1, 1),
    (@ModuleId, @Q1, N'Εσύ', 0, 2),
    (@ModuleId, @Q1, N'Αυτός', 0, 3),
    (@ModuleId, @Q1, N'Εμείς', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Η λέξη "βιβλίο" σημαίνει "book" στα αγγλικά.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Η λέξη "βιβλίο" σημαίνει "book" στα αγγλικά.', @GreekId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'Αληθές', 1, 1),
    (@ModuleId, @Q2, N'Ψευδές', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε το σωστό: Πώς λέμε "hello" στα ελληνικά;')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε το σωστό: Πώς λέμε "hello" στα ελληνικά;', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'Γεια', 1, 1),
    (@ModuleId, @Q3, N'Ευχαριστώ', 0, 2),
    (@ModuleId, @Q3, N'Παρακαλώ', 0, 3),
    (@ModuleId, @Q3, N'Αντίο', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Τι χρώμα είναι ο ουρανός μια καθαρή μέρα;')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Τι χρώμα είναι ο ουρανός μια καθαρή μέρα;', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'μπλε', 1, 1),
    (@ModuleId, @Q4, N'πράσινο', 0, 2),
    (@ModuleId, @Q4, N'κόκκινο', 0, 3),
    (@ModuleId, @Q4, N'κίτρινο', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "Good morning!"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "Good morning!"', @GreekId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'Καλημέρα', 1, 1),
    (@ModuleId, @Q5, N'Καλημέρα!', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τον αριθμό: πέντε + τρία = ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τον αριθμό: πέντε + τρία = ___.', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'επτά', 0, 1),
    (@ModuleId, @Q6, N'οκτώ', 1, 2),
    (@ModuleId, @Q6, N'εννέα', 0, 3),
    (@ModuleId, @Q6, N'έξι', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Η λέξη "σκύλος" σημαίνει "cat" στα αγγλικά.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Η λέξη "σκύλος" σημαίνει "cat" στα αγγλικά.', @GreekId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'Αληθές', 0, 1),
    (@ModuleId, @Q7, N'Ψευδές', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή λέξη: Αυτό είναι ___ σπίτι.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή λέξη: Αυτό είναι ___ σπίτι.', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'ένα', 1, 1),
    (@ModuleId, @Q8, N'μία', 0, 2),
    (@ModuleId, @Q8, N'έναν', 0, 3),
    (@ModuleId, @Q8, N'ενός', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Ποιο είναι το αντίθετο της λέξης "μεγάλος";')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Ποιο είναι το αντίθετο της λέξης "μεγάλος";', @GreekId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'μικρός', 1, 1),
    (@ModuleId, @Q9, N'ψηλός', 0, 2),
    (@ModuleId, @Q9, N'γρήγορος', 0, 3),
    (@ModuleId, @Q9, N'νέος', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "This is a house."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "This is a house."', @GreekId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'Αυτό είναι ένα σπίτι', 1, 1),
    (@ModuleId, @Q10, N'Αυτό είναι ένα σπίτι.', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τον σωστό τύπο: Αυτή ___ στο σχολείο κάθε μέρα.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τον σωστό τύπο: Αυτή ___ στο σχολείο κάθε μέρα.', @GreekId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'πηγαίνει', 1, 1),
    (@ModuleId, @Q11, N'πηγαίνω', 0, 2),
    (@ModuleId, @Q11, N'πηγαίνεις', 0, 3),
    (@ModuleId, @Q11, N'πηγαίνουν', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις λέξεις που είναι φρούτα.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις λέξεις που είναι φρούτα.', @GreekId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'μήλο', 1, 1),
    (@ModuleId, @Q12, N'μπανάνα', 1, 2),
    (@ModuleId, @Q12, N'τραπέζι', 0, 3),
    (@ModuleId, @Q12, N'πορτοκάλι', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Η πρόταση "Χτες πάω στο σινεμά" είναι γραμματικά σωστή.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Η πρόταση "Χτες πάω στο σινεμά" είναι γραμματικά σωστή.', @GreekId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'Αληθές', 0, 1),
    (@ModuleId, @Q13, N'Ψευδές', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή πρόταση.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή πρόταση.', @GreekId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'Έχω δύο αδέλφια.', 1, 1),
    (@ModuleId, @Q14, N'Έχεις δύο αδέλφια εγώ.', 0, 2),
    (@ModuleId, @Q14, N'Έχουμε δύο αδέλφια αυτός.', 0, 3),
    (@ModuleId, @Q14, N'Αδέλφια δύο έχω εγώ.', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "Yesterday I watched a film."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "Yesterday I watched a film."', @GreekId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'Χτες είδα μια ταινία', 1, 1),
    (@ModuleId, @Q15, N'Χτες παρακολούθησα μια ταινία', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή ερώτηση: ___ μένεις;')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή ερώτηση: ___ μένεις;', @GreekId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'Πού', 1, 1),
    (@ModuleId, @Q16, N'Τι', 0, 2),
    (@ModuleId, @Q16, N'Ποιος', 0, 3),
    (@ModuleId, @Q16, N'Πότε', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις λέξεις που περιγράφουν τον καιρό.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις λέξεις που περιγράφουν τον καιρό.', @GreekId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'ηλιόλουστος', 1, 1),
    (@ModuleId, @Q17, N'βροχερός', 1, 2),
    (@ModuleId, @Q17, N'πεινασμένος', 0, 3),
    (@ModuleId, @Q17, N'ανεμώδης', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Η λέξη "αδελφή" σημαίνει "brother" στα αγγλικά.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Η λέξη "αδελφή" σημαίνει "brother" στα αγγλικά.', @GreekId, @A2, @TrueFalse, @Vocabulary, 2, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'Αληθές', 0, 1),
    (@ModuleId, @Q18, N'Ψευδές', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τον σωστό τύπο: Χτες ___ ποδόσφαιρο.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τον σωστό τύπο: Χτες ___ ποδόσφαιρο.', @GreekId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'παίξαμε', 1, 1),
    (@ModuleId, @Q19, N'παίζουμε', 0, 2),
    (@ModuleId, @Q19, N'παίζαμε', 0, 3),
    (@ModuleId, @Q19, N'παίξουμε', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "I like swimming."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "I like swimming."', @GreekId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'Μου αρέσει η κολύμβηση', 1, 1),
    (@ModuleId, @Q20, N'Μου αρέσει να κολυμπάω', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή επιλογή: Αν ___ περισσότερο χρόνο, θα ταξίδευα παραπάνω.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή επιλογή: Αν ___ περισσότερο χρόνο, θα ταξίδευα παραπάνω.', @GreekId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q21 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q21, N'είχα', 1, 1),
    (@ModuleId, @Q21, N'έχω', 0, 2),
    (@ModuleId, @Q21, N'θα έχω', 0, 3),
    (@ModuleId, @Q21, N'έχει', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις λέξεις που σχετίζονται με την εργασία.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις λέξεις που σχετίζονται με την εργασία.', @GreekId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q22 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q22, N'υπάλληλος', 1, 1),
    (@ModuleId, @Q22, N'μισθός', 1, 2),
    (@ModuleId, @Q22, N'διακοπές', 0, 3),
    (@ModuleId, @Q22, N'γραφείο', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Η φράση "Ζω εδώ εδώ και πέντε χρόνια" σημαίνει ότι ο ομιλητής εξακολουθεί να ζει εδώ.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Η φράση "Ζω εδώ εδώ και πέντε χρόνια" σημαίνει ότι ο ομιλητής εξακολουθεί να ζει εδώ.', @GreekId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q23 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q23, N'Αληθές', 1, 1),
    (@ModuleId, @Q23, N'Ψευδές', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "I think this is a good idea."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "I think this is a good idea."', @GreekId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q24 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q24, N'Νομίζω ότι αυτή είναι μια καλή ιδέα', 1, 1),
    (@ModuleId, @Q24, N'Κατά τη γνώμη μου αυτή είναι μια καλή ιδέα', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Γράψε μια πρόταση για τις τελευταίες σου διακοπές.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Γράψε μια πρόταση για τις τελευταίες σου διακοπές.', @GreekId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q25 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q25, N'πήγα', 1, 1),
    (@ModuleId, @Q25, N'επισκέφτηκα', 1, 2),
    (@ModuleId, @Q25, N'έμεινα', 1, 3);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη σωστή επιλογή: Όταν έφτασε, ___ για μια ώρα.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη σωστή επιλογή: Όταν έφτασε, ___ για μια ώρα.', @GreekId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q26 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q26, N'περιμέναμε', 1, 1),
    (@ModuleId, @Q26, N'περιμένουμε', 0, 2),
    (@ModuleId, @Q26, N'θα περιμένουμε', 0, 3),
    (@ModuleId, @Q26, N'περιμείναμε', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις λέξεις που μπορούν να ακολουθήσουν τη λέξη "κάνω".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις λέξεις που μπορούν να ακολουθήσουν τη λέξη "κάνω".', @GreekId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q27 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q27, N'βόλτα', 1, 1),
    (@ModuleId, @Q27, N'μάθημα', 1, 2),
    (@ModuleId, @Q27, N'ταξίδι', 0, 3),
    (@ModuleId, @Q27, N'γυμναστική', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Αληθές ή ψευδές: Το "έκανα" είναι αόριστος του ρήματος "κάνω".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Αληθές ή ψευδές: Το "έκανα" είναι αόριστος του ρήματος "κάνω".', @GreekId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q28 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q28, N'Αληθές', 1, 1),
    (@ModuleId, @Q28, N'Ψευδές', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "I have been learning Greek for a long time."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "I have been learning Greek for a long time."', @GreekId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q29 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q29, N'Μαθαίνω ελληνικά εδώ και πολύ καιρό', 1, 1),
    (@ModuleId, @Q29, N'Σπουδάζω ελληνικά εδώ και πολύ καιρό', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Γράψε μια πρόταση για το τι θα έκανες αν κέρδιζες στο λαχείο.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Γράψε μια πρόταση για το τι θα έκανες αν κέρδιζες στο λαχείο.', @GreekId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q30 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q30, N'θα αγόραζα', 1, 1),
    (@ModuleId, @Q30, N'θα ταξίδευα', 1, 2),
    (@ModuleId, @Q30, N'θα έδινα', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 6 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε το καλύτερο συνώνυμο για τη λέξη "σημαντικός".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε το καλύτερο συνώνυμο για τη λέξη "σημαντικός".', @GreekId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q31 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q31, N'ουσιώδης', 1, 1),
    (@ModuleId, @Q31, N'συνηθισμένος', 0, 2),
    (@ModuleId, @Q31, N'μικρός', 0, 3),
    (@ModuleId, @Q31, N'ασαφής', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις επίσημες εκφράσεις.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις επίσημες εκφράσεις.', @GreekId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q32 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q32, N'Θα ήθελα να ζητήσω...', 1, 1),
    (@ModuleId, @Q32, N'Θα μπορούσατε παρακαλώ...', 1, 2),
    (@ModuleId, @Q32, N'Θα το κάνω τώρα', 0, 3),
    (@ModuleId, @Q32, N'Γεια ρε φίλε', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "Although I was tired, I finished the work."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "Although I was tired, I finished the work."', @GreekId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q33 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q33, N'Αν και ήμουν κουρασμένος, τελείωσα τη δουλειά', 1, 1),
    (@ModuleId, @Q33, N'Παρόλο που ήμουν κουρασμένος, τελείωσα τη δουλειά', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε το καλύτερο συνώνυμο για τη λέξη "ασαφής".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε το καλύτερο συνώνυμο για τη λέξη "ασαφής".', @GreekId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q34 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q34, N'αμφίσημος', 1, 1),
    (@ModuleId, @Q34, N'σαφής', 0, 2),
    (@ModuleId, @Q34, N'απλός', 0, 3),
    (@ModuleId, @Q34, N'χαρούμενος', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε όλες τις προτάσεις που χρησιμοποιούν σωστά την παθητική σύνταξη.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε όλες τις προτάσεις που χρησιμοποιούν σωστά την παθητική σύνταξη.', @GreekId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q35 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q35, N'Η έκθεση γράφτηκε από τη Σάρα.', 1, 1),
    (@ModuleId, @Q35, N'Το κέικ έχει φαγωθεί.', 1, 2),
    (@ModuleId, @Q35, N'Αυτοί έχτισαν τη γέφυρα.', 0, 3),
    (@ModuleId, @Q35, N'Το παράθυρο σπάστηκε από τη μπάλα.', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Μετάφρασε στα ελληνικά: "I wish I had more time to study."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Μετάφρασε στα ελληνικά: "I wish I had more time to study."', @GreekId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q36 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q36, N'Μακάρι να είχα περισσότερο χρόνο να μελετώ', 1, 1),
    (@ModuleId, @Q36, N'Θα ήθελα να είχα περισσότερο χρόνο να μελετώ', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 4 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη λέξη που συμπληρώνει καλύτερα την πρόταση: Το επιχείρημά του ήταν πειστικό, αν και κάπως ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη λέξη που συμπληρώνει καλύτερα την πρόταση: Το επιχείρημά του ήταν πειστικό, αν και κάπως ___.', @GreekId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q37 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q37, N'ελλιπές', 1, 1),
    (@ModuleId, @Q37, N'χαρούμενο', 0, 2),
    (@ModuleId, @Q37, N'ξύλινο', 0, 3),
    (@ModuleId, @Q37, N'σιωπηλό', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Γράψε μια σύνθετη πρόταση χρησιμοποιώντας τη λέξη "ωστόσο".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Γράψε μια σύνθετη πρόταση χρησιμοποιώντας τη λέξη "ωστόσο".', @GreekId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q38 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q38, N'ωστόσο', 1, 1);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Επίλεξε τη λέξη που ταιριάζει καλύτερα: Η νέα πολιτική αντιμετωπίστηκε με ___ αντίθεση από το κοινό.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Επίλεξε τη λέξη που ταιριάζει καλύτερα: Η νέα πολιτική αντιμετωπίστηκε με ___ αντίθεση από το κοινό.', @GreekId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q39 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q39, N'σφοδρή', 1, 1),
    (@ModuleId, @Q39, N'χαρούμενη', 0, 2),
    (@ModuleId, @Q39, N'στενή', 0, 3),
    (@ModuleId, @Q39, N'σιωπηλή', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @GreekId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'el');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Γράψε μια σύνθετη πρόταση χρησιμοποιώντας τη λέξη "εντούτοις".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Γράψε μια σύνθετη πρόταση χρησιμοποιώντας τη λέξη "εντούτοις".', @GreekId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q40 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q40, N'εντούτοις', 1, 1);
END
GO
/* =========================================================
   Source: chinese.sql -> language code zh
   ========================================================= */
/* ---------------------------------------------
   English level test question seed - Chinese Version
   Module-specific seed - 40 Questions
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @EnglishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @EnglishId = LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';

/* ---------------------------------------------
   A1 questions - 10 items
   --------------------------------------------- */

-- Q1
IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请选择正确的单词：How ___ you?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请选择正确的单词：How ___ you?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q1 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q1, N'am', 0, 1), (@ModuleId, @Q1, N'is', 0, 2), (@ModuleId, @Q1, N'are', 1, 3), (@ModuleId, @Q1, N'be', 0, 4);
END
GO

-- Q2
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'判断对错："Apple" 的意思是 "苹果"。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'判断对错："Apple" 的意思是 "苹果"。', @EnglishId, @A1, @TrueFalse, @Vocabulary, 1, 1);
    DECLARE @Q2 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q2, N'正确', 1, 1), (@ModuleId, @Q2, N'错误', 0, 2);
END
GO

-- Q3
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请选择正确的复数：one man, two ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请选择正确的复数：one man, two ___.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q3 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q3, N'mans', 0, 1), (@ModuleId, @Q3, N'men', 1, 2), (@ModuleId, @Q3, N'mens', 0, 3), (@ModuleId, @Q3, N'man', 0, 4);
END
GO

-- Q4
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请问 "Goodbye" 的意思是？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请问 "Goodbye" 的意思是？', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q4 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q4, N'你好', 0, 1), (@ModuleId, @Q4, N'再见', 1, 2), (@ModuleId, @Q4, N'谢谢', 0, 3), (@ModuleId, @Q4, N'不客气', 0, 4);
END
GO

-- Q5
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'翻译成英语："我叫李明。"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'翻译成英语："我叫李明。"', @EnglishId, @A1, @Translation, @Practice, 1, 1);
    DECLARE @Q5 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q5, N'My name is Li Ming', 1, 1), (@ModuleId, @Q5, N'I am Li Ming', 1, 2);
END
GO

-- Q6
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'数字 "Twelve" 是多少？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'数字 "Twelve" 是多少？', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q6 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q6, N'2', 0, 1), (@ModuleId, @Q6, N'12', 1, 2), (@ModuleId, @Q6, N'20', 0, 3), (@ModuleId, @Q6, N'22', 0, 4);
END
GO

-- Q7
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'周一（Monday）之后是哪一天？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'周一（Monday）之后是哪一天？', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q7 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q7, N'Tuesday', 1, 1), (@ModuleId, @Q7, N'Wednesday', 0, 2), (@ModuleId, @Q7, N'Sunday', 0, 3), (@ModuleId, @Q7, N'Friday', 0, 4);
END
GO

-- Q8
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有的颜色单词：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有的颜色单词：', @EnglishId, @A1, @MultipleChoice, @Vocabulary, 1, 1);
    DECLARE @Q8 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q8, N'Red', 1, 1), (@ModuleId, @Q8, N'Blue', 1, 2), (@ModuleId, @Q8, N'Pen', 0, 3), (@ModuleId, @Q8, N'Green', 1, 4);
END
GO

-- Q9
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出正确句子：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出正确句子：', @EnglishId, @A1, @SingleChoice, @Practice, 1, 1);
    DECLARE @Q9 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q9, N'I have a book.', 1, 1), (@ModuleId, @Q9, N'I has a book.', 0, 2), (@ModuleId, @Q9, N'I having a book.', 0, 3), (@ModuleId, @Q9, N'I is a book.', 0, 4);
END
GO

-- Q10
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Doctor" 工作的地方通常是？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'"Doctor" 工作的地方通常是？', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q10 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q10, N'School', 0, 1), (@ModuleId, @Q10, N'Hospital', 1, 2), (@ModuleId, @Q10, N'Park', 0, 3), (@ModuleId, @Q10, N'Cinema', 0, 4);
END
GO

/* ---------------------------------------------
   A2 questions - 10 items
   --------------------------------------------- */

-- Q11
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请选择正确词形：He ___ TV every evening.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请选择正确词形：He ___ TV every evening.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q11 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q11, N'watch', 0, 1), (@ModuleId, @Q11, N'watches', 1, 2), (@ModuleId, @Q11, N'watching', 0, 3), (@ModuleId, @Q11, N'watched', 0, 4);
END
GO

-- Q12
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有与“交通”相关的单词：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有与“交通”相关的单词：', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q12 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q12, N'Bus', 1, 1), (@ModuleId, @Q12, N'Train', 1, 2), (@ModuleId, @Q12, N'Bread', 0, 3), (@ModuleId, @Q12, N'Bicycle', 1, 4);
END
GO

-- Q13
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'判断对错："I am more tall than you" 是正确的语法。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'判断对错："I am more tall than you" 是正确的语法。', @EnglishId, @A2, @TrueFalse, @Practice, 2, 1);
    DECLARE @Q13 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q13, N'正确', 0, 1), (@ModuleId, @Q13, N'错误', 1, 2);
END
GO

-- Q14
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择正确问句：___ did you do last weekend?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择正确问句：___ did you do last weekend?', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q14 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q14, N'Who', 0, 1), (@ModuleId, @Q14, N'Where', 0, 2), (@ModuleId, @Q14, N'What', 1, 3), (@ModuleId, @Q14, N'When', 0, 4);
END
GO

-- Q15
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'翻译成英语："他比我大。"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'翻译成英语："他比我大。"', @EnglishId, @A2, @Translation, @Practice, 2, 1);
    DECLARE @Q15 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q15, N'He is older than me', 1, 1), (@ModuleId, @Q15, N'He is elder than me', 1, 2);
END
GO

-- Q16
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出正确的过去式：I ___ to the cinema yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出正确的过去式：I ___ to the cinema yesterday.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q16 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q16, N'go', 0, 1), (@ModuleId, @Q16, N'went', 1, 2), (@ModuleId, @Q16, N'gone', 0, 3), (@ModuleId, @Q16, N'goes', 0, 4);
END
GO

-- Q17
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'哪一个单词是 "Quickly" 的反义词？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'哪一个单词是 "Quickly" 的反义词？', @EnglishId, @A2, @SingleChoice, @Vocabulary, 2, 1);
    DECLARE @Q17 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q17, N'Fastly', 0, 1), (@ModuleId, @Q17, N'Slowly', 1, 2), (@ModuleId, @Q17, N'Happily', 0, 3), (@ModuleId, @Q17, N'Quietly', 0, 4);
END
GO

-- Q18
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出介词：There is a book ___ the table.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出介词：There is a book ___ the table.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q18 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q18, N'on', 1, 1), (@ModuleId, @Q18, N'at', 0, 2), (@ModuleId, @Q18, N'to', 0, 3), (@ModuleId, @Q18, N'in', 0, 4);
END
GO

-- Q19
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有表示“感觉”的单词：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有表示“感觉”的单词：', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q19 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q19, N'Happy', 1, 1), (@ModuleId, @Q19, N'Sad', 1, 2), (@ModuleId, @Q19, N'Angry', 1, 3), (@ModuleId, @Q19, N'Wall', 0, 4);
END
GO

-- Q20
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'I would like ___ water, please.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'I would like ___ water, please.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q20 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q20, N'a', 0, 1), (@ModuleId, @Q20, N'an', 0, 2), (@ModuleId, @Q20, N'some', 1, 3), (@ModuleId, @Q20, N'any', 0, 4);
END
GO

/* ---------------------------------------------
   B1 questions - 10 items
   --------------------------------------------- */

-- Q21
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请选择正确时态：I ___ English for three years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请选择正确时态：I ___ English for three years.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q21 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q21, N'study', 0, 1), (@ModuleId, @Q21, N'have studied', 1, 2), (@ModuleId, @Q21, N'am studying', 0, 3), (@ModuleId, @Q21, N'will study', 0, 4);
END
GO

-- Q22
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'判断对错："If it rains, I will stay at home" 是第一条件句。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'判断对错："If it rains, I will stay at home" 是第一条件句。', @EnglishId, @B1, @TrueFalse, @Reading, 3, 1);
    DECLARE @Q22 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q22, N'正确', 1, 1), (@ModuleId, @Q22, N'错误', 0, 2);
END
GO

-- Q23
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有可以搭配 "Take" 的词组：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有可以搭配 "Take" 的词组：', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q23 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q23, N'a photo', 1, 1), (@ModuleId, @Q23, N'a mistake', 0, 2), (@ModuleId, @Q23, N'a bus', 1, 3), (@ModuleId, @Q23, N'an exam', 1, 4);
END
GO

-- Q24
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'翻译成英语："你不必现在就走。"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'翻译成英语："你不必现在就走。"', @EnglishId, @B1, @Translation, @Practice, 3, 1);
    DECLARE @Q24 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q24, N'You don''t have to go now', 1, 1), (@ModuleId, @Q24, N'You needn''t go now', 1, 2);
END
GO

-- Q25
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出正确的被动语态：The letter ___ by me yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出正确的被动语态：The letter ___ by me yesterday.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q25 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q25, N'was written', 1, 1), (@ModuleId, @Q25, N'writes', 0, 2), (@ModuleId, @Q25, N'was write', 0, 3), (@ModuleId, @Q25, N'wrote', 0, 4);
END
GO

-- Q26
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Although" 的同义词是？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'"Although" 的同义词是？', @EnglishId, @B1, @SingleChoice, @Vocabulary, 3, 1);
    DECLARE @Q26 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q26, N'However', 0, 1), (@ModuleId, @Q26, N'Even though', 1, 2), (@ModuleId, @Q26, N'Because', 0, 3), (@ModuleId, @Q26, N'But', 0, 4);
END
GO

-- Q27
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = 'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'用英语写一个关于你周末计划的句子。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'用英语写一个关于你周末计划的句子。', @EnglishId, @B1, @Essay, @Practice, 3, 1);
    DECLARE @Q27 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q27, N'I am going to', 1, 1), (@ModuleId, @Q27, N'I will', 1, 2);
END
GO

-- Q28
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出正确连接词：I stayed at home ___ it was raining.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出正确连接词：I stayed at home ___ it was raining.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q28 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q28, N'because', 1, 1), (@ModuleId, @Q28, N'so', 0, 2), (@ModuleId, @Q28, N'but', 0, 3), (@ModuleId, @Q28, N'although', 0, 4);
END
GO

-- Q29
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'He is the man ___ lives next door.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'He is the man ___ lives next door.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q29 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q29, N'who', 1, 1), (@ModuleId, @Q29, N'which', 0, 2), (@ModuleId, @Q29, N'whom', 0, 3), (@ModuleId, @Q29, N'whose', 0, 4);
END
GO

-- Q30
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有描述“性格”的形容词：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有描述“性格”的形容词：', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q30 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q30, N'Kind', 1, 1), (@ModuleId, @Q30, N'Selfish', 1, 2), (@ModuleId, @Q30, N'Polite', 1, 3), (@ModuleId, @Q30, N'Square', 0, 4);
END
GO

/* ---------------------------------------------
   B2 questions - 6 items
   --------------------------------------------- */

-- Q31
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct form: If I ___ you, I would take the job.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Choose the correct form: If I ___ you, I would take the job.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q31 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q31, N'am', 0, 1), (@ModuleId, @Q31, N'was', 0, 2), (@ModuleId, @Q31, N'were', 1, 3), (@ModuleId, @Q31, N'be', 0, 4);
END
GO

-- Q32
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选出 "Inevitable" 的最接近含义：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选出 "Inevitable" 的最接近含义：', @EnglishId, @B2, @SingleChoice, @Vocabulary, 4, 1);
    DECLARE @Q32 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q32, N'Unavoidable', 1, 1), (@ModuleId, @Q32, N'Possible', 0, 2), (@ModuleId, @Q32, N'Rare', 0, 3), (@ModuleId, @Q32, N'Exciting', 0, 4);
END
GO

-- Q33
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'翻译成英语："他否认拿了那笔钱。"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'翻译成英语："他否认拿了那笔钱。"', @EnglishId, @B2, @Translation, @Practice, 4, 1);
    DECLARE @Q33 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q33, N'He denied taking the money', 1, 1), (@ModuleId, @Q33, N'He denied having taken the money', 1, 2);
END
GO

-- Q34
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct option: By next year, I ___ here for ten years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Choose the correct option: By next year, I ___ here for ten years.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q34 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q34, N'will have lived', 1, 1), (@ModuleId, @Q34, N'will live', 0, 2), (@ModuleId, @Q34, N'have lived', 0, 3), (@ModuleId, @Q34, N'am living', 0, 4);
END
GO

-- Q35
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'选择所有正式（Formal）的短语：')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'选择所有正式（Formal）的短语：', @EnglishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);
    DECLARE @Q35 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q35, N'I am writing to inform you...', 1, 1), (@ModuleId, @Q35, N'What''s up?', 0, 2), (@ModuleId, @Q35, N'Sincerely yours', 1, 3), (@ModuleId, @Q35, N'I would appreciate it if...', 1, 4);
END
GO

-- Q36
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Which sentence is correct?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Which sentence is correct?', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q36 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q36, N'Hardly had I arrived when it started raining.', 1, 1), (@ModuleId, @Q36, N'Hardly I arrived when it started raining.', 0, 2), (@ModuleId, @Q36, N'Hardly had I arrived than it started raining.', 0, 3), (@ModuleId, @Q36, N'I had hardly arrive when it started raining.', 0, 4);
END
GO

/* ---------------------------------------------
   C1 questions - 4 items
   --------------------------------------------- */

-- Q37
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the best word: The manager’s decision was ___ with controversy.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Choose the best word: The manager’s decision was ___ with controversy.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q37 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q37, N'fraught', 1, 1), (@ModuleId, @Q37, N'filled', 0, 2), (@ModuleId, @Q37, N'covered', 0, 3), (@ModuleId, @Q37, N'joined', 0, 4);
END
GO

-- Q38
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'翻译成英语："无论结果如何，我们都必须继续。"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'翻译成英语："无论结果如何，我们都必须继续。"', @EnglishId, @C1, @Translation, @Practice, 5, 1);
    DECLARE @Q38 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q38, N'Regardless of the outcome, we must continue', 1, 1), (@ModuleId, @Q38, N'No matter what the result is, we must go on', 1, 2);
END
GO

-- Q39
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Choose the correct idiom: He decided to ___ and tell the truth.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Choose the correct idiom: He decided to ___ and tell the truth.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q39 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q39, N'come clean', 1, 1), (@ModuleId, @Q39, N'go white', 0, 2), (@ModuleId, @Q39, N'make blue', 0, 3), (@ModuleId, @Q39, N'get red', 0, 4);
END
GO

-- Q40
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'zh');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'请用英语写一段话（不少于两句），讨论人工智能对教育的影响。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'请用英语写一段话（不少于两句），讨论人工智能对教育的影响。', @EnglishId, @C1, @Essay, @Practice, 5, 1);
    DECLARE @Q40 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q40, N'AI', 1, 1);
END
GO
/* =========================================================
   Source: horvat.sql -> language code hr
   ========================================================= */
/* ---------------------------------------------
   English level test question seed - Croatian Version
   Module-specific seed - 40 Questions
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @EnglishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @EnglishId = LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';

/* ---------------------------------------------
   A1 pitanja - 10 stavki
   --------------------------------------------- */

-- Q1
IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točnu riječ: How ___ you?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točnu riječ: How ___ you?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q1 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q1, N'am', 0, 1), (@ModuleId, @Q1, N'is', 0, 2), (@ModuleId, @Q1, N'are', 1, 3), (@ModuleId, @Q1, N'be', 0, 4);
END
GO

-- Q2
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Točno ili netočno: Riječ "Apple" znači "jabuka".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Točno ili netočno: Riječ "Apple" znači "jabuka".', @EnglishId, @A1, @TrueFalse, @Vocabulary, 1, 1);
    DECLARE @Q2 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q2, N'Točno', 1, 1), (@ModuleId, @Q2, N'Netočno', 0, 2);
END
GO

-- Q3
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točnu množinu: one man, two ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točnu množinu: one man, two ___.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q3 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q3, N'mans', 0, 1), (@ModuleId, @Q3, N'men', 1, 2), (@ModuleId, @Q3, N'mens', 0, 3), (@ModuleId, @Q3, N'man', 0, 4);
END
GO

-- Q4
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Što znači riječ "Goodbye"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Što znači riječ "Goodbye"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q4 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q4, N'Bok (pri dolasku)', 0, 1), (@ModuleId, @Q4, N'Doviđenja', 1, 2), (@ModuleId, @Q4, N'Hvala', 0, 3), (@ModuleId, @Q4, N'Molim', 0, 4);
END
GO

-- Q5
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Prevedi na engleski: "Zovem se Ivan."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Prevedi na engleski: "Zovem se Ivan."', @EnglishId, @A1, @Translation, @Practice, 1, 1);
    DECLARE @Q5 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q5, N'My name is Ivan', 1, 1), (@ModuleId, @Q5, N'I am Ivan', 1, 2);
END
GO

-- Q6
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Koji je broj "Twelve"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Koji je broj "Twelve"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q6 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q6, N'2', 0, 1), (@ModuleId, @Q6, N'12', 1, 2), (@ModuleId, @Q6, N'20', 0, 3), (@ModuleId, @Q6, N'22', 0, 4);
END
GO

-- Q7
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Koji dan dolazi nakon ponedjeljka (Monday)?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Koji dan dolazi nakon ponedjeljka (Monday)?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q7 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q7, N'Tuesday', 1, 1), (@ModuleId, @Q7, N'Wednesday', 0, 2), (@ModuleId, @Q7, N'Sunday', 0, 3), (@ModuleId, @Q7, N'Friday', 0, 4);
END
GO

-- Q8
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve riječi koje označavaju boje:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve riječi koje označavaju boje:', @EnglishId, @A1, @MultipleChoice, @Vocabulary, 1, 1);
    DECLARE @Q8 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q8, N'Red', 1, 1), (@ModuleId, @Q8, N'Blue', 1, 2), (@ModuleId, @Q8, N'Pen', 0, 3), (@ModuleId, @Q8, N'Green', 1, 4);
END
GO

-- Q9
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi gramatički točnu rečenicu:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi gramatički točnu rečenicu:', @EnglishId, @A1, @SingleChoice, @Practice, 1, 1);
    DECLARE @Q9 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q9, N'I have a book.', 1, 1), (@ModuleId, @Q9, N'I has a book.', 0, 2), (@ModuleId, @Q9, N'I having a book.', 0, 3), (@ModuleId, @Q9, N'I is a book.', 0, 4);
END
GO

-- Q10
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Gdje obično radi "Doctor"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Gdje obično radi "Doctor"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q10 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q10, N'School', 0, 1), (@ModuleId, @Q10, N'Hospital', 1, 2), (@ModuleId, @Q10, N'Park', 0, 3), (@ModuleId, @Q10, N'Cinema', 0, 4);
END
GO

/* ---------------------------------------------
   A2 pitanja - 10 stavki
   --------------------------------------------- */

-- Q11
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan oblik glagola: He ___ TV every evening.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan oblik glagola: He ___ TV every evening.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q11 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q11, N'watch', 0, 1), (@ModuleId, @Q11, N'watches', 1, 2), (@ModuleId, @Q11, N'watching', 0, 3), (@ModuleId, @Q11, N'watched', 0, 4);
END
GO

-- Q12
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve riječi povezane s "transportom":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve riječi povezane s "transportom":', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q12 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q12, N'Bus', 1, 1), (@ModuleId, @Q12, N'Train', 1, 2), (@ModuleId, @Q12, N'Bread', 0, 3), (@ModuleId, @Q12, N'Bicycle', 1, 4);
END
GO

-- Q13
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Točno ili netočno: "I am more tall than you" je gramatički točno.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Točno ili netočno: "I am more tall than you" je gramatički točno.', @EnglishId, @A2, @TrueFalse, @Practice, 2, 1);
    DECLARE @Q13 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q13, N'Točno', 0, 1), (@ModuleId, @Q13, N'Netočno', 1, 2);
END
GO

-- Q14
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točnu upitnu zamjenicu: ___ did you do last weekend?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točnu upitnu zamjenicu: ___ did you do last weekend?', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q14 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q14, N'Who', 0, 1), (@ModuleId, @Q14, N'Where', 0, 2), (@ModuleId, @Q14, N'What', 1, 3), (@ModuleId, @Q14, N'When', 0, 4);
END
GO

-- Q15
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Prevedi na engleski: "On je stariji od mene."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Prevedi na engleski: "On je stariji od mene."', @EnglishId, @A2, @Translation, @Practice, 2, 1);
    DECLARE @Q15 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q15, N'He is older than me', 1, 1), (@ModuleId, @Q15, N'He is elder than me', 1, 2);
END
GO

-- Q16
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan prošli oblik: I ___ to the cinema yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan prošli oblik: I ___ to the cinema yesterday.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q16 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q16, N'go', 0, 1), (@ModuleId, @Q16, N'went', 1, 2), (@ModuleId, @Q16, N'gone', 0, 3), (@ModuleId, @Q16, N'goes', 0, 4);
END
GO

-- Q17
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Koja je riječ suprotna od "Quickly"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Koja je riječ suprotna od "Quickly"?', @EnglishId, @A2, @SingleChoice, @Vocabulary, 2, 1);
    DECLARE @Q17 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q17, N'Fastly', 0, 1), (@ModuleId, @Q17, N'Slowly', 1, 2), (@ModuleId, @Q17, N'Happily', 0, 3), (@ModuleId, @Q17, N'Quietly', 0, 4);
END
GO

-- Q18
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan prijedlog: There is a book ___ the table.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan prijedlog: There is a book ___ the table.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q18 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q18, N'on', 1, 1), (@ModuleId, @Q18, N'at', 0, 2), (@ModuleId, @Q18, N'to', 0, 3), (@ModuleId, @Q18, N'in', 0, 4);
END
GO

-- Q19
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve riječi koje opisuju "osjećaje":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve riječi koje opisuju "osjećaje":', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q19 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q19, N'Happy', 1, 1), (@ModuleId, @Q19, N'Sad', 1, 2), (@ModuleId, @Q19, N'Angry', 1, 3), (@ModuleId, @Q19, N'Wall', 0, 4);
END
GO

-- Q20
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'I would like ___ water, please.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'I would like ___ water, please.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q20 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q20, N'a', 0, 1), (@ModuleId, @Q20, N'an', 0, 2), (@ModuleId, @Q20, N'some', 1, 3), (@ModuleId, @Q20, N'any', 0, 4);
END
GO

/* ---------------------------------------------
   B1 pitanja - 10 stavki
   --------------------------------------------- */

-- Q21
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točno glagolsko vrijeme: I ___ English for three years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točno glagolsko vrijeme: I ___ English for three years.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q21 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q21, N'study', 0, 1), (@ModuleId, @Q21, N'have studied', 1, 2), (@ModuleId, @Q21, N'am studying', 0, 3), (@ModuleId, @Q21, N'will study', 0, 4);
END
GO

-- Q22
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Točno ili netočno: "If it rains, I will stay at home" je prvi kondicional.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Točno ili netočno: "If it rains, I will stay at home" je prvi kondicional.', @EnglishId, @B1, @TrueFalse, @Reading, 3, 1);
    DECLARE @Q22 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q22, N'Točno', 1, 1), (@ModuleId, @Q22, N'Netočno', 0, 2);
END
GO

-- Q23
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve fraze koje se mogu koristiti s glagolom "Take":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve fraze koje se mogu koristiti s glagolom "Take":', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q23 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q23, N'a photo', 1, 1), (@ModuleId, @Q23, N'a mistake', 0, 2), (@ModuleId, @Q23, N'a bus', 1, 3), (@ModuleId, @Q23, N'an exam', 1, 4);
END
GO

-- Q24
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Prevedi na engleski: "Ne moraš ići sada."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Prevedi na engleski: "Ne moraš ići sada."', @EnglishId, @B1, @Translation, @Practice, 3, 1);
    DECLARE @Q24 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q24, N'You don''t have to go now', 1, 1), (@ModuleId, @Q24, N'You needn''t go now', 1, 2);
END
GO

-- Q25
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan pasivni oblik: The letter ___ by me yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan pasivni oblik: The letter ___ by me yesterday.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q25 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q25, N'was written', 1, 1), (@ModuleId, @Q25, N'writes', 0, 2), (@ModuleId, @Q25, N'was write', 0, 3), (@ModuleId, @Q25, N'wrote', 0, 4);
END
GO

-- Q26
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Koji je sinonim za riječ "Although"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Koji je sinonim za riječ "Although"?', @EnglishId, @B1, @SingleChoice, @Vocabulary, 3, 1);
    DECLARE @Q26 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q26, N'However', 0, 1), (@ModuleId, @Q26, N'Even though', 1, 2), (@ModuleId, @Q26, N'Because', 0, 3), (@ModuleId, @Q26, N'But', 0, 4);
END
GO

-- Q27
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Napiši jednu rečenicu na engleskom o tvojim planovima za vikend.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Napiši jednu rečenicu na engleskom o tvojim planovima za vikend.', @EnglishId, @B1, @Essay, @Practice, 3, 1);
    DECLARE @Q27 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q27, N'I am going to', 1, 1), (@ModuleId, @Q27, N'I will', 1, 2);
END
GO

-- Q28
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan veznik: I stayed at home ___ it was raining.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan veznik: I stayed at home ___ it was raining.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q28 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q28, N'because', 1, 1), (@ModuleId, @Q28, N'so', 0, 2), (@ModuleId, @Q28, N'but', 0, 3), (@ModuleId, @Q28, N'although', 0, 4);
END
GO

-- Q29
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'He is the man ___ lives next door.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'He is the man ___ lives next door.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q29 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q29, N'who', 1, 1), (@ModuleId, @Q29, N'which', 0, 2), (@ModuleId, @Q29, N'whom', 0, 3), (@ModuleId, @Q29, N'whose', 0, 4);
END
GO

-- Q30
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve pridjeve koji opisuju "karakter":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve pridjeve koji opisuju "karakter":', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q30 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q30, N'Kind', 1, 1), (@ModuleId, @Q30, N'Selfish', 1, 2), (@ModuleId, @Q30, N'Polite', 1, 3), (@ModuleId, @Q30, N'Square', 0, 4);
END
GO

/* ---------------------------------------------
   B2 pitanja - 6 stavki
   --------------------------------------------- */

-- Q31
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan oblik: If I ___ you, I would take the job.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan oblik: If I ___ you, I would take the job.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q31 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q31, N'am', 0, 1), (@ModuleId, @Q31, N'was', 0, 2), (@ModuleId, @Q31, N'were', 1, 3), (@ModuleId, @Q31, N'be', 0, 4);
END
GO

-- Q32
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Što najbolje opisuje značenje riječi "Inevitable"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Što najbolje opisuje značenje riječi "Inevitable"?', @EnglishId, @B2, @SingleChoice, @Vocabulary, 4, 1);
    DECLARE @Q32 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q32, N'Unavoidable (neizbježno)', 1, 1), (@ModuleId, @Q32, N'Possible', 0, 2), (@ModuleId, @Q32, N'Rare', 0, 3), (@ModuleId, @Q32, N'Exciting', 0, 4);
END
GO

-- Q33
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Prevedi na engleski: "On je porekao da je uzeo novac."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Prevedi na engleski: "On je porekao da je uzeo novac."', @EnglishId, @B2, @Translation, @Practice, 4, 1);
    DECLARE @Q33 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q33, N'He denied taking the money', 1, 1), (@ModuleId, @Q33, N'He denied having taken the money', 1, 2);
END
GO

-- Q34
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točnu opciju: By next year, I ___ here for ten years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točnu opciju: By next year, I ___ here for ten years.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q34 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q34, N'will have lived', 1, 1), (@ModuleId, @Q34, N'will live', 0, 2), (@ModuleId, @Q34, N'have lived', 0, 3), (@ModuleId, @Q34, N'am living', 0, 4);
END
GO

-- Q35
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi sve formalne (Formal) fraze:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi sve formalne (Formal) fraze:', @EnglishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);
    DECLARE @Q35 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q35, N'I am writing to inform you...', 1, 1), (@ModuleId, @Q35, N'What''s up?', 0, 2), (@ModuleId, @Q35, N'Sincerely yours', 1, 3), (@ModuleId, @Q35, N'I would appreciate it if...', 1, 4);
END
GO

-- Q36
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Koja je rečenica točna?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Koja je rečenica točna?', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q36 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q36, N'Hardly had I arrived when it started raining.', 1, 1), (@ModuleId, @Q36, N'Hardly I arrived when it started raining.', 0, 2), (@ModuleId, @Q36, N'Hardly had I arrived than it started raining.', 0, 3), (@ModuleId, @Q36, N'I had hardly arrive when it started raining.', 0, 4);
END
GO

/* ---------------------------------------------
   C1 pitanja - 4 stavke
   --------------------------------------------- */

-- Q37
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi najbolju riječ: The manager’s decision was ___ with controversy.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi najbolju riječ: The manager’s decision was ___ with controversy.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q37 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q37, N'fraught', 1, 1), (@ModuleId, @Q37, N'filled', 0, 2), (@ModuleId, @Q37, N'covered', 0, 3), (@ModuleId, @Q37, N'joined', 0, 4);
END
GO

-- Q38
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Prevedi na engleski: "Bez obzira na ishod, moramo nastaviti."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Prevedi na engleski: "Bez obzira na ishod, moramo nastaviti."', @EnglishId, @C1, @Translation, @Practice, 5, 1);
    DECLARE @Q38 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q38, N'Regardless of the outcome, we must continue', 1, 1), (@ModuleId, @Q38, N'No matter what the result is, we must go on', 1, 2);
END
GO

-- Q39
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Odaberi točan idiom: He decided to ___ and tell the truth.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Odaberi točan idiom: He decided to ___ and tell the truth.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q39 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q39, N'come clean', 1, 1), (@ModuleId, @Q39, N'go white', 0, 2), (@ModuleId, @Q39, N'make blue', 0, 3), (@ModuleId, @Q39, N'get red', 0, 4);
END
GO

-- Q40
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'hr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Napiši kratak tekst na engleskom (barem dvije rečenice) o utjecaju umjetne inteligencije na obrazovanje.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Napiši kratak tekst na engleskom (barem dvije rečenice) o utjecaju umjetne inteligencije na obrazovanje.', @EnglishId, @C1, @Essay, @Practice, 5, 1);
    DECLARE @Q40 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q40, N'AI', 1, 1);
END
GO
/* =========================================================
   Source: japanise.sql -> language code ja
   ========================================================= */
/* ---------------------------------------------
   Japanese level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @JapaneseId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @JapaneseId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'ja';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* ---------------------------------------------
   A1 questions - 10 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい言葉を選んでください：わたし___ がくせいです。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい言葉を選んでください：わたし___ がくせいです。', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'は', 1, 1),
    (@ModuleId, @Q1, N'が', 0, 2),
    (@ModuleId, @Q1, N'を', 0, 3),
    (@ModuleId, @Q1, N'に', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：「ほん」は英語で "book" という意味です。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：「ほん」は英語で "book" という意味です。', @JapaneseId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'正しい', 1, 1),
    (@ModuleId, @Q2, N'間違い', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「hello」は日本語で何と言いますか？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「hello」は日本語で何と言いますか？', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'こんにちは', 1, 1),
    (@ModuleId, @Q3, N'ありがとう', 0, 2),
    (@ModuleId, @Q3, N'おねがいします', 0, 3),
    (@ModuleId, @Q3, N'さようなら', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'晴れた日に空は何色ですか？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'晴れた日に空は何色ですか？', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'あおい', 1, 1),
    (@ModuleId, @Q4, N'みどり', 0, 2),
    (@ModuleId, @Q4, N'あかい', 0, 3),
    (@ModuleId, @Q4, N'きいろい', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："Good morning!"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："Good morning!"', @JapaneseId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'おはようございます', 1, 1),
    (@ModuleId, @Q5, N'おはよう', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい数字を選んでください：ご + さん = ___。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい数字を選んでください：ご + さん = ___。', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'なな', 0, 1),
    (@ModuleId, @Q6, N'はち', 1, 2),
    (@ModuleId, @Q6, N'きゅう', 0, 3),
    (@ModuleId, @Q6, N'ろく', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：「いぬ」は英語で "cat" という意味です。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：「いぬ」は英語で "cat" という意味です。', @JapaneseId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'正しい', 0, 1),
    (@ModuleId, @Q7, N'間違い', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「おおきい」の反対語はどれですか？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「おおきい」の反対語はどれですか？', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'ちいさい', 1, 1),
    (@ModuleId, @Q8, N'たかい', 0, 2),
    (@ModuleId, @Q8, N'はやい', 0, 3),
    (@ModuleId, @Q8, N'あたらしい', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい助詞を選んでください：がっこう___ いきます。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい助詞を選んでください：がっこう___ いきます。', @JapaneseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'に', 1, 1),
    (@ModuleId, @Q9, N'は', 0, 2),
    (@ModuleId, @Q9, N'が', 0, 3),
    (@ModuleId, @Q9, N'を', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："This is a house."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："This is a house."', @JapaneseId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'これはいえです', 1, 1),
    (@ModuleId, @Q10, N'これは家です', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい形を選んでください：かのじょは まいにち がっこうに ___ 。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい形を選んでください：かのじょは まいにち がっこうに ___ 。', @JapaneseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'いきます', 1, 1),
    (@ModuleId, @Q11, N'いきました', 0, 2),
    (@ModuleId, @Q11, N'いきません', 0, 3),
    (@ModuleId, @Q11, N'いきましょう', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'くだものの言葉をすべて選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'くだものの言葉をすべて選んでください。', @JapaneseId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'りんご', 1, 1),
    (@ModuleId, @Q12, N'バナナ', 1, 2),
    (@ModuleId, @Q12, N'テーブル', 0, 3),
    (@ModuleId, @Q12, N'オレンジ', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：「きのう えいがに いきます」は文法的に正しい文です。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：「きのう えいがに いきます」は文法的に正しい文です。', @JapaneseId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'正しい', 0, 1),
    (@ModuleId, @Q13, N'間違い', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい文を選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい文を選んでください。', @JapaneseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'わたしは きょうだいが ふたり います。', 1, 1),
    (@ModuleId, @Q14, N'わたしは きょうだいを ふたり います。', 0, 2),
    (@ModuleId, @Q14, N'わたしに きょうだいが ふたり あります。', 0, 3),
    (@ModuleId, @Q14, N'わたしが きょうだいは ふたり です。', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："Yesterday I watched a film."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："Yesterday I watched a film."', @JapaneseId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'きのう えいがを みました', 1, 1),
    (@ModuleId, @Q15, N'昨日 映画を 見ました', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい疑問詞を選んでください：___ に すんでいますか？')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい疑問詞を選んでください：___ に すんでいますか？', @JapaneseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'どこ', 1, 1),
    (@ModuleId, @Q16, N'なに', 0, 2),
    (@ModuleId, @Q16, N'だれ', 0, 3),
    (@ModuleId, @Q16, N'いつ', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'てんきを表す言葉をすべて選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'てんきを表す言葉をすべて選んでください。', @JapaneseId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'はれ', 1, 1),
    (@ModuleId, @Q17, N'あめ', 1, 2),
    (@ModuleId, @Q17, N'おなかがすいた', 0, 3),
    (@ModuleId, @Q17, N'かぜ', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：「いもうと」は英語で "brother" という意味です。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：「いもうと」は英語で "brother" という意味です。', @JapaneseId, @A2, @TrueFalse, @Vocabulary, 2, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'正しい', 0, 1),
    (@ModuleId, @Q18, N'間違い', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい形を選んでください：きのう サッカーを ___ 。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい形を選んでください：きのう サッカーを ___ 。', @JapaneseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'しました', 1, 1),
    (@ModuleId, @Q19, N'します', 0, 2),
    (@ModuleId, @Q19, N'しています', 0, 3),
    (@ModuleId, @Q19, N'しましょう', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："I like swimming."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："I like swimming."', @JapaneseId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'わたしは およぐのが すきです', 1, 1),
    (@ModuleId, @Q20, N'私は 泳ぐのが 好きです', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい選択肢を選んでください：もし じかんが ___ もっと りょこうするのに。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい選択肢を選んでください：もし じかんが ___ もっと りょこうするのに。', @JapaneseId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q21 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q21, N'あれば', 1, 1),
    (@ModuleId, @Q21, N'あります', 0, 2),
    (@ModuleId, @Q21, N'あった', 0, 3),
    (@ModuleId, @Q21, N'ある', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'しごとに関係する言葉をすべて選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'しごとに関係する言葉をすべて選んでください。', @JapaneseId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q22 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q22, N'しゃいん', 1, 1),
    (@ModuleId, @Q22, N'きゅうりょう', 1, 2),
    (@ModuleId, @Q22, N'やすみ', 0, 3),
    (@ModuleId, @Q22, N'オフィス', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：「ここに ごねん すんでいます」は話者がまだここに住んでいることを意味します。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：「ここに ごねん すんでいます」は話者がまだここに住んでいることを意味します。', @JapaneseId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q23 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q23, N'正しい', 1, 1),
    (@ModuleId, @Q23, N'間違い', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："I think this is a good idea."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："I think this is a good idea."', @JapaneseId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q24 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q24, N'これは いいアイデアだと おもいます', 1, 1),
    (@ModuleId, @Q24, N'これは 良いアイデアだと 思います', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'さいごの休みについて一文書いてください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'さいごの休みについて一文書いてください。', @JapaneseId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q25 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q25, N'いきました', 1, 1),
    (@ModuleId, @Q25, N'たずねました', 1, 2),
    (@ModuleId, @Q25, N'たのしみました', 1, 3);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しい選択肢を選んでください：かのじょが きたとき わたしたちは いちじかん ___ 。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しい選択肢を選んでください：かのじょが きたとき わたしたちは いちじかん ___ 。', @JapaneseId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q26 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q26, N'まっていました', 1, 1),
    (@ModuleId, @Q26, N'まっています', 0, 2),
    (@ModuleId, @Q26, N'まちます', 0, 3),
    (@ModuleId, @Q26, N'まちました', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「する」のあとに続けられる言葉をすべて選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「する」のあとに続けられる言葉をすべて選んでください。', @JapaneseId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q27 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q27, N'べんきょう', 1, 1),
    (@ModuleId, @Q27, N'りょうり', 1, 2),
    (@ModuleId, @Q27, N'たびをする', 0, 3),
    (@ModuleId, @Q27, N'うんどう', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'正しいか間違いか：日本語の「〜ていた」は過去の継続した動作を表します。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'正しいか間違いか：日本語の「〜ていた」は過去の継続した動作を表します。', @JapaneseId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q28 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q28, N'正しい', 1, 1),
    (@ModuleId, @Q28, N'間違い', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："I have been learning Japanese for a long time."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："I have been learning Japanese for a long time."', @JapaneseId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q29 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q29, N'ながい あいだ にほんごを べんきょうしています', 1, 1),
    (@ModuleId, @Q29, N'長い間 日本語を 勉強しています', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'もし宝くじに当たったら何をしますか？一文書いてください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'もし宝くじに当たったら何をしますか？一文書いてください。', @JapaneseId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q30 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q30, N'かうでしょう', 1, 1),
    (@ModuleId, @Q30, N'たびするでしょう', 1, 2),
    (@ModuleId, @Q30, N'あげるでしょう', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 6 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「じゅうよう」の最もよい類義語を選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「じゅうよう」の最もよい類義語を選んでください。', @JapaneseId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q31 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q31, N'かんじん', 1, 1),
    (@ModuleId, @Q31, N'ふつう', 0, 2),
    (@ModuleId, @Q31, N'ちいさい', 0, 3),
    (@ModuleId, @Q31, N'あいまい', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'すべての丁寧な表現を選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'すべての丁寧な表現を選んでください。', @JapaneseId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q32 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q32, N'おねがいしてもよろしいでしょうか', 1, 1),
    (@ModuleId, @Q32, N'〜していただけますか', 1, 2),
    (@ModuleId, @Q32, N'すぐやる', 0, 3),
    (@ModuleId, @Q32, N'ちょっと待ってよ', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："Although I was tired, I finished the work."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："Although I was tired, I finished the work."', @JapaneseId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q33 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q33, N'つかれていたけれど しごとを おえました', 1, 1),
    (@ModuleId, @Q33, N'疲れていたにもかかわらず 仕事を終えました', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「あいまい」の最もよい類義語を選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「あいまい」の最もよい類義語を選んでください。', @JapaneseId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q34 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q34, N'はっきりしない', 1, 1),
    (@ModuleId, @Q34, N'めいかく', 0, 2),
    (@ModuleId, @Q34, N'かんたん', 0, 3),
    (@ModuleId, @Q34, N'たのしい', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'受け身の形を正しく使っている文をすべて選んでください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'受け身の形を正しく使っている文をすべて選んでください。', @JapaneseId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q35 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q35, N'そのほうこくは サラに かかれました。', 1, 1),
    (@ModuleId, @Q35, N'ケーキは たべられました。', 1, 2),
    (@ModuleId, @Q35, N'かれらは はしを たてました。', 0, 3),
    (@ModuleId, @Q35, N'まどは ボールに われました。', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'日本語に訳してください："I wish I had more time to study."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'日本語に訳してください："I wish I had more time to study."', @JapaneseId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q36 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q36, N'もっと べんきょうする じかんが あればいいのに', 1, 1),
    (@ModuleId, @Q36, N'もっと勉強する時間があれば良いのに', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 4 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'文を最もよく完成させる言葉を選んでください：かれの ろんは せっとくりょくが あったが やや ___ だった。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'文を最もよく完成させる言葉を選んでください：かれの ろんは せっとくりょくが あったが やや ___ だった。', @JapaneseId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q37 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q37, N'けっかんだらけ', 1, 1),
    (@ModuleId, @Q37, N'たのしい', 0, 2),
    (@ModuleId, @Q37, N'しずか', 0, 3),
    (@ModuleId, @Q37, N'もくせい', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「それにもかかわらず」という言葉を使って複雑な文を一つ書いてください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「それにもかかわらず」という言葉を使って複雑な文を一つ書いてください。', @JapaneseId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q38 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q38, N'それにもかかわらず', 1, 1);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'最も適切な言葉を選んでください：あたらしい せいさくは こくみんから ___ はんたいに あった。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'最も適切な言葉を選んでください：あたらしい せいさくは こくみんから ___ はんたいに あった。', @JapaneseId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q39 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q39, N'はげしい', 1, 1),
    (@ModuleId, @Q39, N'たのしい', 0, 2),
    (@ModuleId, @Q39, N'せまい', 0, 3),
    (@ModuleId, @Q39, N'しずかな', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @JapaneseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ja');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'「とはいえ」という言葉を使って複雑な文を一つ書いてください。')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'「とはいえ」という言葉を使って複雑な文を一つ書いてください。', @JapaneseId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q40 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q40, N'とはいえ', 1, 1);
END
GO
/* =========================================================
   Source: protugease.sql -> language code pt
   ========================================================= */
/* ---------------------------------------------
   Portuguese level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @PortugueseId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @PortugueseId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'pt';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* ---------------------------------------------
   A1 questions - 10 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a palavra correta: Eu ___ estudante.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a palavra correta: Eu ___ estudante.', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'sou', 1, 1),
    (@ModuleId, @Q1, N'és', 0, 2),
    (@ModuleId, @Q1, N'é', 0, 3),
    (@ModuleId, @Q1, N'somos', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: A palavra "livro" significa "book" em inglês.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: A palavra "livro" significa "book" em inglês.', @PortugueseId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'Verdadeiro', 1, 1),
    (@ModuleId, @Q2, N'Falso', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Como se diz "hello" em português?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Como se diz "hello" em português?', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'Olá', 1, 1),
    (@ModuleId, @Q3, N'Obrigado', 0, 2),
    (@ModuleId, @Q3, N'Por favor', 0, 3),
    (@ModuleId, @Q3, N'Tchau', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Que cor é o céu num dia limpo?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Que cor é o céu num dia limpo?', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'azul', 1, 1),
    (@ModuleId, @Q4, N'verde', 0, 2),
    (@ModuleId, @Q4, N'vermelho', 0, 3),
    (@ModuleId, @Q4, N'amarelo', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "Good morning!"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "Good morning!"', @PortugueseId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'Bom dia', 1, 1),
    (@ModuleId, @Q5, N'Bom dia!', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha o número correto: cinco + três = ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha o número correto: cinco + três = ___.', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'sete', 0, 1),
    (@ModuleId, @Q6, N'oito', 1, 2),
    (@ModuleId, @Q6, N'nove', 0, 3),
    (@ModuleId, @Q6, N'seis', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: A palavra "cão" significa "cat" em inglês.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: A palavra "cão" significa "cat" em inglês.', @PortugueseId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'Verdadeiro', 0, 1),
    (@ModuleId, @Q7, N'Falso', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a palavra correta: Isto é ___ casa.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a palavra correta: Isto é ___ casa.', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'uma', 1, 1),
    (@ModuleId, @Q8, N'um', 0, 2),
    (@ModuleId, @Q8, N'umas', 0, 3),
    (@ModuleId, @Q8, N'uns', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Qual é o oposto da palavra "grande"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Qual é o oposto da palavra "grande"?', @PortugueseId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'pequeno', 1, 1),
    (@ModuleId, @Q9, N'alto', 0, 2),
    (@ModuleId, @Q9, N'rápido', 0, 3),
    (@ModuleId, @Q9, N'novo', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "This is a house."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "This is a house."', @PortugueseId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'Isto é uma casa', 1, 1),
    (@ModuleId, @Q10, N'Esta é uma casa', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a forma correta: Ela ___ para a escola todos os dias.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a forma correta: Ela ___ para a escola todos os dias.', @PortugueseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'vai', 1, 1),
    (@ModuleId, @Q11, N'vou', 0, 2),
    (@ModuleId, @Q11, N'vais', 0, 3),
    (@ModuleId, @Q11, N'vão', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as palavras que são frutas.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as palavras que são frutas.', @PortugueseId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'maçã', 1, 1),
    (@ModuleId, @Q12, N'banana', 1, 2),
    (@ModuleId, @Q12, N'mesa', 0, 3),
    (@ModuleId, @Q12, N'laranja', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: A frase "Ontem eu vou ao cinema" está gramaticalmente correta.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: A frase "Ontem eu vou ao cinema" está gramaticalmente correta.', @PortugueseId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'Verdadeiro', 0, 1),
    (@ModuleId, @Q13, N'Falso', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a frase correta.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a frase correta.', @PortugueseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'Eu tenho dois irmãos.', 1, 1),
    (@ModuleId, @Q14, N'Eu tem dois irmãos.', 0, 2),
    (@ModuleId, @Q14, N'Eu tendo dois irmãos.', 0, 3),
    (@ModuleId, @Q14, N'Eu sou ter dois irmãos.', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "Yesterday I watched a film."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "Yesterday I watched a film."', @PortugueseId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'Ontem eu vi um filme', 1, 1),
    (@ModuleId, @Q15, N'Ontem eu assisti a um filme', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a pergunta correta: ___ você mora?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a pergunta correta: ___ você mora?', @PortugueseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'Onde', 1, 1),
    (@ModuleId, @Q16, N'O que', 0, 2),
    (@ModuleId, @Q16, N'Quem', 0, 3),
    (@ModuleId, @Q16, N'Quando', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as palavras que descrevem o tempo meteorológico.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as palavras que descrevem o tempo meteorológico.', @PortugueseId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'ensolarado', 1, 1),
    (@ModuleId, @Q17, N'chuvoso', 1, 2),
    (@ModuleId, @Q17, N'com fome', 0, 3),
    (@ModuleId, @Q17, N'ventoso', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: A palavra "irmã" significa "brother" em inglês.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: A palavra "irmã" significa "brother" em inglês.', @PortugueseId, @A2, @TrueFalse, @Vocabulary, 2, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'Verdadeiro', 0, 1),
    (@ModuleId, @Q18, N'Falso', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a forma correta: Ontem nós ___ futebol.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a forma correta: Ontem nós ___ futebol.', @PortugueseId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'jogámos', 1, 1),
    (@ModuleId, @Q19, N'jogamos', 1, 2),
    (@ModuleId, @Q19, N'jogamos sempre', 0, 3),
    (@ModuleId, @Q19, N'jogaremos', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "I like swimming."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "I like swimming."', @PortugueseId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'Eu gosto de nadar', 1, 1),
    (@ModuleId, @Q20, N'Gosto de natação', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a opção correta: Se eu ___ mais tempo, viajaria mais.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a opção correta: Se eu ___ mais tempo, viajaria mais.', @PortugueseId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q21 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q21, N'tivesse', 1, 1),
    (@ModuleId, @Q21, N'tenho', 0, 2),
    (@ModuleId, @Q21, N'terei', 0, 3),
    (@ModuleId, @Q21, N'tem', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as palavras relacionadas com o trabalho.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as palavras relacionadas com o trabalho.', @PortugueseId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q22 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q22, N'funcionário', 1, 1),
    (@ModuleId, @Q22, N'salário', 1, 2),
    (@ModuleId, @Q22, N'férias', 0, 3),
    (@ModuleId, @Q22, N'escritório', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: "Moro aqui há cinco anos" significa que o falante ainda mora aqui.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: "Moro aqui há cinco anos" significa que o falante ainda mora aqui.', @PortugueseId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q23 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q23, N'Verdadeiro', 1, 1),
    (@ModuleId, @Q23, N'Falso', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "I think this is a good idea."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "I think this is a good idea."', @PortugueseId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q24 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q24, N'Acho que esta é uma boa ideia', 1, 1),
    (@ModuleId, @Q24, N'Na minha opinião esta é uma boa ideia', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escreva uma frase sobre as suas últimas férias.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escreva uma frase sobre as suas últimas férias.', @PortugueseId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q25 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q25, N'fui', 1, 1),
    (@ModuleId, @Q25, N'visitei', 1, 2),
    (@ModuleId, @Q25, N'fiquei', 1, 3);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a opção correta: Quando ela chegou, nós já ___ há uma hora.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a opção correta: Quando ela chegou, nós já ___ há uma hora.', @PortugueseId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q26 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q26, N'esperávamos', 1, 1),
    (@ModuleId, @Q26, N'esperamos', 0, 2),
    (@ModuleId, @Q26, N'vamos esperar', 0, 3),
    (@ModuleId, @Q26, N'esperar', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as palavras que podem seguir o verbo "fazer".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as palavras que podem seguir o verbo "fazer".',  @PortugueseId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q27 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q27, N'uma pergunta', 1, 1),
    (@ModuleId, @Q27, N'um erro', 1, 2),
    (@ModuleId, @Q27, N'uma viagem', 0, 3),
    (@ModuleId, @Q27, N'exercício', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Verdadeiro ou falso: O pretérito imperfeito descreve ações habituais no passado.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Verdadeiro ou falso: O pretérito imperfeito descreve ações habituais no passado.', @PortugueseId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q28 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q28, N'Verdadeiro', 1, 1),
    (@ModuleId, @Q28, N'Falso', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "I have been learning Portuguese for a long time."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "I have been learning Portuguese for a long time."', @PortugueseId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q29 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q29, N'Estou a aprender português há muito tempo', 1, 1),
    (@ModuleId, @Q29, N'Aprendo português há muito tempo', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escreva uma frase sobre o que faria se ganhasse na lotaria.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escreva uma frase sobre o que faria se ganhasse na lotaria.', @PortugueseId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q30 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q30, N'compraria', 1, 1),
    (@ModuleId, @Q30, N'viajaria', 1, 2),
    (@ModuleId, @Q30, N'daria', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 6 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha o melhor sinónimo para a palavra "significativo".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha o melhor sinónimo para a palavra "significativo".', @PortugueseId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q31 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q31, N'relevante', 1, 1),
    (@ModuleId, @Q31, N'comum', 0, 2),
    (@ModuleId, @Q31, N'minúsculo', 0, 3),
    (@ModuleId, @Q31, N'confuso', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as expressões formais.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as expressões formais.', @PortugueseId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q32 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q32, N'Gostaria de solicitar...', 1, 1),
    (@ModuleId, @Q32, N'Poderia por favor...', 1, 2),
    (@ModuleId, @Q32, N'Vou fazer já', 0, 3),
    (@ModuleId, @Q32, N'Ei amigo', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "Although I was tired, I finished the work."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "Although I was tired, I finished the work."', @PortugueseId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q33 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q33, N'Embora estivesse cansado, terminei o trabalho', 1, 1),
    (@ModuleId, @Q33, N'Apesar de estar cansado, terminei o trabalho', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha o melhor sinónimo para a palavra "ambíguo".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha o melhor sinónimo para a palavra "ambíguo".', @PortugueseId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q34 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q34, N'impreciso', 1, 1),
    (@ModuleId, @Q34, N'certo', 0, 2),
    (@ModuleId, @Q34, N'simples', 0, 3),
    (@ModuleId, @Q34, N'alegre', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Selecione todas as frases que usam corretamente a voz passiva.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Selecione todas as frases que usam corretamente a voz passiva.', @PortugueseId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q35 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q35, N'O relatório foi escrito pela Sara.', 1, 1),
    (@ModuleId, @Q35, N'O bolo foi comido.', 1, 2),
    (@ModuleId, @Q35, N'Eles construíram a ponte.', 0, 3),
    (@ModuleId, @Q35, N'A janela foi partida pela bola.', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Traduza para o português: "I wish I had more time to study."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Traduza para o português: "I wish I had more time to study."', @PortugueseId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q36 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q36, N'Queria ter mais tempo para estudar', 1, 1),
    (@ModuleId, @Q36, N'Oxalá tivesse mais tempo para estudar', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 4 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a palavra que melhor completa a frase: O seu argumento era persuasivo, embora um tanto ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a palavra que melhor completa a frase: O seu argumento era persuasivo, embora um tanto ___.', @PortugueseId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q37 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q37, N'falacioso', 1, 1),
    (@ModuleId, @Q37, N'alegre', 0, 2),
    (@ModuleId, @Q37, N'lenhoso', 0, 3),
    (@ModuleId, @Q37, N'silencioso', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escreva uma frase complexa usando a palavra "não obstante".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escreva uma frase complexa usando a palavra "não obstante".', @PortugueseId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q38 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q38, N'não obstante', 1, 1);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escolha a palavra que melhor se adequa: A nova política foi recebida com ___ oposição por parte do público.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escolha a palavra que melhor se adequa: A nova política foi recebida com ___ oposição por parte do público.', @PortugueseId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q39 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q39, N'veemente', 1, 1),
    (@ModuleId, @Q39, N'alegre', 0, 2),
    (@ModuleId, @Q39, N'estreita', 0, 3),
    (@ModuleId, @Q39, N'silenciosa', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @PortugueseId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'pt');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Escreva uma frase complexa usando a palavra "todavia".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Escreva uma frase complexa usando a palavra "todavia".', @PortugueseId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q40 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q40, N'todavia', 1, 1);
END
GO
/* =========================================================
   Source: russion.sql -> language code ru
   ========================================================= */
/* ---------------------------------------------
   English level test question seed - Russian Version
   Module-specific seed - 40 Questions
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @EnglishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @EnglishId = LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';

/* ---------------------------------------------
   Вопросы уровня A1 - 10 шт.
   --------------------------------------------- */

-- Q1
IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильное слово: How ___ you?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильное слово: How ___ you?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q1 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q1, N'am', 0, 1), (@ModuleId, @Q1, N'is', 0, 2), (@ModuleId, @Q1, N'are', 1, 3), (@ModuleId, @Q1, N'be', 0, 4);
END
GO

-- Q2
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Верно или неверно: Слово "Apple" означает "яблоко".')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Верно или неверно: Слово "Apple" означает "яблоко".', @EnglishId, @A1, @TrueFalse, @Vocabulary, 1, 1);
    DECLARE @Q2 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q2, N'Верно', 1, 1), (@ModuleId, @Q2, N'Неверно', 0, 2);
END
GO

-- Q3
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильное множественное число: one man, two ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильное множественное число: one man, two ___.', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q3 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q3, N'mans', 0, 1), (@ModuleId, @Q3, N'men', 1, 2), (@ModuleId, @Q3, N'mens', 0, 3), (@ModuleId, @Q3, N'man', 0, 4);
END
GO

-- Q4
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Что означает слово "Goodbye"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Что означает слово "Goodbye"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q4 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q4, N'Привет', 0, 1), (@ModuleId, @Q4, N'До свидания', 1, 2), (@ModuleId, @Q4, N'Спасибо', 0, 3), (@ModuleId, @Q4, N'Пожалуйста', 0, 4);
END
GO

-- Q5
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Переведите на английский: "Меня зовут Иван."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Переведите на английский: "Меня зовут Иван."', @EnglishId, @A1, @Translation, @Practice, 1, 1);
    DECLARE @Q5 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q5, N'My name is Ivan', 1, 1), (@ModuleId, @Q5, N'I am Ivan', 1, 2);
END
GO

-- Q6
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Какое это число "Twelve"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Какое это число "Twelve"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q6 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q6, N'2', 0, 1), (@ModuleId, @Q6, N'12', 1, 2), (@ModuleId, @Q6, N'20', 0, 3), (@ModuleId, @Q6, N'22', 0, 4);
END
GO

-- Q7
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Какой день идет после понедельника (Monday)?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Какой день идет после понедельника (Monday)?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q7 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q7, N'Tuesday', 1, 1), (@ModuleId, @Q7, N'Wednesday', 0, 2), (@ModuleId, @Q7, N'Sunday', 0, 3), (@ModuleId, @Q7, N'Friday', 0, 4);
END
GO

-- Q8
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все названия цветов:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все названия цветов:', @EnglishId, @A1, @MultipleChoice, @Vocabulary, 1, 1);
    DECLARE @Q8 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q8, N'Red', 1, 1), (@ModuleId, @Q8, N'Blue', 1, 2), (@ModuleId, @Q8, N'Pen', 0, 3), (@ModuleId, @Q8, N'Green', 1, 4);
END
GO

-- Q9
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите грамматически верное предложение:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите грамматически верное предложение:', @EnglishId, @A1, @SingleChoice, @Practice, 1, 1);
    DECLARE @Q9 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q9, N'I have a book.', 1, 1), (@ModuleId, @Q9, N'I has a book.', 0, 2), (@ModuleId, @Q9, N'I having a book.', 0, 3), (@ModuleId, @Q9, N'I is a book.', 0, 4);
END
GO

-- Q10
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Где обычно работает "Doctor"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Где обычно работает "Doctor"?', @EnglishId, @A1, @SingleChoice, @Vocabulary, 1, 1);
    DECLARE @Q10 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q10, N'School', 0, 1), (@ModuleId, @Q10, N'Hospital', 1, 2), (@ModuleId, @Q10, N'Park', 0, 3), (@ModuleId, @Q10, N'Cinema', 0, 4);
END
GO

/* ---------------------------------------------
   Вопросы уровня A2 - 10 шт.
   --------------------------------------------- */

-- Q11
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильную форму: He ___ TV every evening.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильную форму: He ___ TV every evening.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q11 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q11, N'watch', 0, 1), (@ModuleId, @Q11, N'watches', 1, 2), (@ModuleId, @Q11, N'watching', 0, 3), (@ModuleId, @Q11, N'watched', 0, 4);
END
GO

-- Q12
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все слова, связанные с транспортом:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все слова, связанные с транспортом:', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q12 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q12, N'Bus', 1, 1), (@ModuleId, @Q12, N'Train', 1, 2), (@ModuleId, @Q12, N'Bread', 0, 3), (@ModuleId, @Q12, N'Bicycle', 1, 4);
END
GO

-- Q13
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Верно или неверно: "I am more tall than you" — это грамматически верно.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Верно или неверно: "I am more tall than you" — это грамматически верно.', @EnglishId, @A2, @TrueFalse, @Practice, 2, 1);
    DECLARE @Q13 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q13, N'Верно', 0, 1), (@ModuleId, @Q13, N'Неверно', 1, 2);
END
GO

-- Q14
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите вопросительное слово: ___ did you do last weekend?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите вопросительное слово: ___ did you do last weekend?', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q14 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q14, N'Who', 0, 1), (@ModuleId, @Q14, N'Where', 0, 2), (@ModuleId, @Q14, N'What', 1, 3), (@ModuleId, @Q14, N'When', 0, 4);
END
GO

-- Q15
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Переведите на английский: "Он старше меня."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Переведите на английский: "Он старше меня."', @EnglishId, @A2, @Translation, @Practice, 2, 1);
    DECLARE @Q15 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q15, N'He is older than me', 1, 1), (@ModuleId, @Q15, N'He is elder than me', 1, 2);
END
GO

-- Q16
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильную форму прошедшего времени: I ___ to the cinema yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильную форму прошедшего времени: I ___ to the cinema yesterday.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q16 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q16, N'go', 0, 1), (@ModuleId, @Q16, N'went', 1, 2), (@ModuleId, @Q16, N'gone', 0, 3), (@ModuleId, @Q16, N'goes', 0, 4);
END
GO

-- Q17
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Какое слово является антонимом к "Quickly"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Какое слово является антонимом к "Quickly"?', @EnglishId, @A2, @SingleChoice, @Vocabulary, 2, 1);
    DECLARE @Q17 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q17, N'Fastly', 0, 1), (@ModuleId, @Q17, N'Slowly', 1, 2), (@ModuleId, @Q17, N'Happily', 0, 3), (@ModuleId, @Q17, N'Quietly', 0, 4);
END
GO

-- Q18
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильный предлог: There is a book ___ the table.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильный предлог: There is a book ___ the table.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q18 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q18, N'on', 1, 1), (@ModuleId, @Q18, N'at', 0, 2), (@ModuleId, @Q18, N'to', 0, 3), (@ModuleId, @Q18, N'in', 0, 4);
END
GO

-- Q19
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все слова, описывающие чувства:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все слова, описывающие чувства:', @EnglishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);
    DECLARE @Q19 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q19, N'Happy', 1, 1), (@ModuleId, @Q19, N'Sad', 1, 2), (@ModuleId, @Q19, N'Angry', 1, 3), (@ModuleId, @Q19, N'Wall', 0, 4);
END
GO

-- Q20
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'I would like ___ water, please.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'I would like ___ water, please.', @EnglishId, @A2, @SingleChoice, @Practice, 2, 1);
    DECLARE @Q20 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q20, N'a', 0, 1), (@ModuleId, @Q20, N'an', 0, 2), (@ModuleId, @Q20, N'some', 1, 3), (@ModuleId, @Q20, N'any', 0, 4);
END
GO

/* ---------------------------------------------
   Вопросы уровня B1 - 10 шт.
   --------------------------------------------- */

-- Q21
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильное время: I ___ English for three years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильное время: I ___ English for three years.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q21 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q21, N'study', 0, 1), (@ModuleId, @Q21, N'have studied', 1, 2), (@ModuleId, @Q21, N'am studying', 0, 3), (@ModuleId, @Q21, N'will study', 0, 4);
END
GO

-- Q22
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Верно или неверно: "If it rains, I will stay at home" — это первый тип условных предложений.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Верно или неверно: "If it rains, I will stay at home" — это первый тип условных предложений.', @EnglishId, @B1, @TrueFalse, @Reading, 3, 1);
    DECLARE @Q22 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q22, N'Верно', 1, 1), (@ModuleId, @Q22, N'Неверно', 0, 2);
END
GO

-- Q23
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все фразы, которые сочетаются с глаголом "Take":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все фразы, которые сочетаются с глаголом "Take":', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q23 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q23, N'a photo', 1, 1), (@ModuleId, @Q23, N'a mistake', 0, 2), (@ModuleId, @Q23, N'a bus', 1, 3), (@ModuleId, @Q23, N'an exam', 1, 4);
END
GO

-- Q24
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Переведите на английский: "Тебе не обязательно уходить сейчас."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Переведите на английский: "Тебе не обязательно уходить сейчас."', @EnglishId, @B1, @Translation, @Practice, 3, 1);
    DECLARE @Q24 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q24, N'You don''t have to go now', 1, 1), (@ModuleId, @Q24, N'You needn''t go now', 1, 2);
END
GO

-- Q25
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильный пассивный залог: The letter ___ by me yesterday.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильный пассивный залог: The letter ___ by me yesterday.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q25 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q25, N'was written', 1, 1), (@ModuleId, @Q25, N'writes', 0, 2), (@ModuleId, @Q25, N'was write', 0, 3), (@ModuleId, @Q25, N'wrote', 0, 4);
END
GO

-- Q26
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Какое слово является синонимом к "Although"?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Какое слово является синонимом к "Although"?', @EnglishId, @B1, @SingleChoice, @Vocabulary, 3, 1);
    DECLARE @Q26 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q26, N'However', 0, 1), (@ModuleId, @Q26, N'Even though', 1, 2), (@ModuleId, @Q26, N'Because', 0, 3), (@ModuleId, @Q26, N'But', 0, 4);
END
GO

-- Q27
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Напишите одно предложение на английском языке о ваших планах на выходные.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Напишите одно предложение на английском языке о ваших планах на выходные.', @EnglishId, @B1, @Essay, @Practice, 3, 1);
    DECLARE @Q27 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q27, N'I am going to', 1, 1), (@ModuleId, @Q27, N'I will', 1, 2);
END
GO

-- Q28
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильный союз: I stayed at home ___ it was raining.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильный союз: I stayed at home ___ it was raining.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q28 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q28, N'because', 1, 1), (@ModuleId, @Q28, N'so', 0, 2), (@ModuleId, @Q28, N'but', 0, 3), (@ModuleId, @Q28, N'although', 0, 4);
END
GO

-- Q29
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'He is the man ___ lives next door.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'He is the man ___ lives next door.', @EnglishId, @B1, @SingleChoice, @Practice, 3, 1);
    DECLARE @Q29 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q29, N'who', 1, 1), (@ModuleId, @Q29, N'which', 0, 2), (@ModuleId, @Q29, N'whom', 0, 3), (@ModuleId, @Q29, N'whose', 0, 4);
END
GO

-- Q30
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все прилагательные, описывающие характер:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все прилагательные, описывающие характер:', @EnglishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);
    DECLARE @Q30 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q30, N'Kind', 1, 1), (@ModuleId, @Q30, N'Selfish', 1, 2), (@ModuleId, @Q30, N'Polite', 1, 3), (@ModuleId, @Q30, N'Square', 0, 4);
END
GO

/* ---------------------------------------------
   Вопросы уровня B2 - 6 шт.
   --------------------------------------------- */

-- Q31
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильную форму: If I ___ you, I would take the job.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильную форму: If I ___ you, I would take the job.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q31 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q31, N'am', 0, 1), (@ModuleId, @Q31, N'was', 0, 2), (@ModuleId, @Q31, N'were', 1, 3), (@ModuleId, @Q31, N'be', 0, 4);
END
GO

-- Q32
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите наиболее близкое значение слова "Inevitable":')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите наиболее близкое значение слова "Inevitable":', @EnglishId, @B2, @SingleChoice, @Vocabulary, 4, 1);
    DECLARE @Q32 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q32, N'Unavoidable', 1, 1), (@ModuleId, @Q32, N'Possible', 0, 2), (@ModuleId, @Q32, N'Rare', 0, 3), (@ModuleId, @Q32, N'Exciting', 0, 4);
END
GO

-- Q33
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Переведите на английский: "Он отрицал, что взял деньги."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Переведите на английский: "Он отрицал, что взял деньги."', @EnglishId, @B2, @Translation, @Practice, 4, 1);
    DECLARE @Q33 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q33, N'He denied taking the money', 1, 1), (@ModuleId, @Q33, N'He denied having taken the money', 1, 2);
END
GO

-- Q34
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильный вариант: By next year, I ___ here for ten years.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильный вариант: By next year, I ___ here for ten years.', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q34 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q34, N'will have lived', 1, 1), (@ModuleId, @Q34, N'will live', 0, 2), (@ModuleId, @Q34, N'have lived', 0, 3), (@ModuleId, @Q34, N'am living', 0, 4);
END
GO

-- Q35
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите все официальные (Formal) фразы:')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите все официальные (Formal) фразы:', @EnglishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);
    DECLARE @Q35 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q35, N'I am writing to inform you...', 1, 1), (@ModuleId, @Q35, N'What''s up?', 0, 2), (@ModuleId, @Q35, N'Sincerely yours', 1, 3), (@ModuleId, @Q35, N'I would appreciate it if...', 1, 4);
END
GO

-- Q36
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Какое предложение верно?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Какое предложение верно?', @EnglishId, @B2, @SingleChoice, @Practice, 4, 1);
    DECLARE @Q36 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q36, N'Hardly had I arrived when it started raining.', 1, 1), (@ModuleId, @Q36, N'Hardly I arrived when it started raining.', 0, 2), (@ModuleId, @Q36, N'Hardly had I arrived than it started raining.', 0, 3), (@ModuleId, @Q36, N'I had hardly arrive when it started raining.', 0, 4);
END
GO

/* ---------------------------------------------
   Вопросы уровня C1 - 4 шт.
   --------------------------------------------- */

-- Q37
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите наиболее подходящее слово: The manager’s decision was ___ with controversy.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите наиболее подходящее слово: The manager’s decision was ___ with controversy.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q37 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q37, N'fraught', 1, 1), (@ModuleId, @Q37, N'filled', 0, 2), (@ModuleId, @Q37, N'covered', 0, 3), (@ModuleId, @Q37, N'joined', 0, 4);
END
GO

-- Q38
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Переведите на английский: "Независимо от результата, мы должны продолжать."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Переведите на английский: "Независимо от результата, мы должны продолжать."', @EnglishId, @C1, @Translation, @Practice, 5, 1);
    DECLARE @Q38 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q38, N'Regardless of the outcome, we must continue', 1, 1), (@ModuleId, @Q38, N'No matter what the result is, we must go on', 1, 2);
END
GO

-- Q39
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Выберите правильную идиому: He decided to ___ and tell the truth.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Выберите правильную идиому: He decided to ___ and tell the truth.', @EnglishId, @C1, @SingleChoice, @Vocabulary, 5, 1);
    DECLARE @Q39 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q39, N'come clean', 1, 1), (@ModuleId, @Q39, N'go white', 0, 2), (@ModuleId, @Q39, N'make blue', 0, 3), (@ModuleId, @Q39, N'get red', 0, 4);
END
GO

-- Q40
DECLARE @ModuleId INT = 0;
DECLARE @EnglishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'ru');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Напишите на английском языке абзац (минимум 2 предложения) о влиянии ИИ на образование.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions] (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES (@ModuleId, N'Напишите на английском языке абзац (минимум 2 предложения) о влиянии ИИ на образование.', @EnglishId, @C1, @Essay, @Practice, 5, 1);
    DECLARE @Q40 INT = SCOPE_IDENTITY();
    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES (@ModuleId, @Q40, N'AI', 1, 1);
END
GO
/* =========================================================
   Source: turkish.sql -> language code tr
   ========================================================= */
/* ---------------------------------------------
   Turkish level test question seed
   Module-specific seed
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;

DECLARE @TurkishId INT;
DECLARE @A1 INT;
DECLARE @A2 INT;
DECLARE @B1 INT;
DECLARE @B2 INT;
DECLARE @C1 INT;

DECLARE @SingleChoice INT;
DECLARE @MultipleChoice INT;
DECLARE @TrueFalse INT;
DECLARE @Essay INT;
DECLARE @Translation INT;

DECLARE @Vocabulary INT;
DECLARE @Reading INT;
DECLARE @Practice INT;

SELECT @TurkishId = LanguageId
FROM {databaseOwner}[{objectQualifier}lm_languages]
WHERE Code = N'tr';

SELECT @A1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1';
SELECT @A2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2';
SELECT @B1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1';
SELECT @B2 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2';
SELECT @C1 = QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1';

SELECT @SingleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice';
SELECT @MultipleChoice = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice';
SELECT @TrueFalse = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse';
SELECT @Essay = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay';
SELECT @Translation = QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation';

SELECT @Vocabulary = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary';
SELECT @Reading = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading';
SELECT @Practice = SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice';


/* ---------------------------------------------
   A1 questions - 10 db
   --------------------------------------------- */

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru kelimeyi seçin: Ben bir ___ .')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru kelimeyi seçin: Ben bir ___ .', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q1 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q1, N'öğrenciyim', 1, 1),
    (@ModuleId, @Q1, N'öğrencisin', 0, 2),
    (@ModuleId, @Q1, N'öğrencidir', 0, 3),
    (@ModuleId, @Q1, N'öğrenciyiz', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: "Kitap" kelimesi İngilizce''de "book" demektir.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: "Kitap" kelimesi İngilizce''de "book" demektir.', @TurkishId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q2 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q2, N'Doğru', 1, 1),
    (@ModuleId, @Q2, N'Yanlış', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçede "hello" nasıl denir?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçede "hello" nasıl denir?', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q3 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q3, N'Merhaba', 1, 1),
    (@ModuleId, @Q3, N'Teşekkürler', 0, 2),
    (@ModuleId, @Q3, N'Lütfen', 0, 3),
    (@ModuleId, @Q3, N'Hoşça kal', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Açık bir günde gökyüzü ne renktir?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Açık bir günde gökyüzü ne renktir?', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q4 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q4, N'mavi', 1, 1),
    (@ModuleId, @Q4, N'yeşil', 0, 2),
    (@ModuleId, @Q4, N'kırmızı', 0, 3),
    (@ModuleId, @Q4, N'sarı', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "Good morning!"')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "Good morning!"', @TurkishId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q5 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q5, N'Günaydın', 1, 1),
    (@ModuleId, @Q5, N'Günaydın!', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru sayıyı seçin: beş + üç = ___.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru sayıyı seçin: beş + üç = ___.', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q6 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q6, N'yedi', 0, 1),
    (@ModuleId, @Q6, N'sekiz', 1, 2),
    (@ModuleId, @Q6, N'dokuz', 0, 3),
    (@ModuleId, @Q6, N'altı', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: "Köpek" kelimesi İngilizce''de "cat" demektir.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: "Köpek" kelimesi İngilizce''de "cat" demektir.', @TurkishId, @A1, @TrueFalse, @Vocabulary, 1, 1);

    DECLARE @Q7 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q7, N'Doğru', 0, 1),
    (@ModuleId, @Q7, N'Yanlış', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru kelimeyi seçin: Bu ___ bir ev.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru kelimeyi seçin: Bu ___ bir ev.', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q8 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q8, N'çok güzel', 1, 1),
    (@ModuleId, @Q8, N'çok güzeller', 0, 2),
    (@ModuleId, @Q8, N'çok güzelim', 0, 3),
    (@ModuleId, @Q8, N'çok güzelsin', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Büyük" kelimesinin zıt anlamlısı nedir?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Büyük" kelimesinin zıt anlamlısı nedir?', @TurkishId, @A1, @SingleChoice, @Vocabulary, 1, 1);

    DECLARE @Q9 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q9, N'küçük', 1, 1),
    (@ModuleId, @Q9, N'uzun', 0, 2),
    (@ModuleId, @Q9, N'hızlı', 0, 3),
    (@ModuleId, @Q9, N'yeni', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "This is a house."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "This is a house."', @TurkishId, @A1, @Translation, @Practice, 1, 1);

    DECLARE @Q10 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q10, N'Bu bir ev', 1, 1),
    (@ModuleId, @Q10, N'Bu bir evdir', 1, 2);
END
GO


/* ---------------------------------------------
   A2 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru biçimi seçin: O her gün okula ___ .')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru biçimi seçin: O her gün okula ___ .', @TurkishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q11 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q11, N'gider', 1, 1),
    (@ModuleId, @Q11, N'gidiyorum', 0, 2),
    (@ModuleId, @Q11, N'gidersin', 0, 3),
    (@ModuleId, @Q11, N'giderler', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Meyve olan tüm kelimeleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Meyve olan tüm kelimeleri seçin.', @TurkishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q12 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q12, N'elma', 1, 1),
    (@ModuleId, @Q12, N'muz', 1, 2),
    (@ModuleId, @Q12, N'masa', 0, 3),
    (@ModuleId, @Q12, N'portakal', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: "Dün sinemaya gidiyorum" cümlesi dilbilgisi açısından doğrudur.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: "Dün sinemaya gidiyorum" cümlesi dilbilgisi açısından doğrudur.', @TurkishId, @A2, @TrueFalse, @Practice, 2, 1);

    DECLARE @Q13 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q13, N'Doğru', 0, 1),
    (@ModuleId, @Q13, N'Yanlış', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru cümleyi seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru cümleyi seçin.', @TurkishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q14 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q14, N'İki kardeşim var.', 1, 1),
    (@ModuleId, @Q14, N'İki kardeşim vardır bende.', 0, 2),
    (@ModuleId, @Q14, N'Kardeşim iki var.', 0, 3),
    (@ModuleId, @Q14, N'Ben iki kardeş sahip.', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "Yesterday I watched a film."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "Yesterday I watched a film."', @TurkishId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q15 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q15, N'Dün bir film izledim', 1, 1),
    (@ModuleId, @Q15, N'Dün film seyrettim', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru soruyu seçin: ___ oturuyorsunuz?')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru soruyu seçin: ___ oturuyorsunuz?', @TurkishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q16 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q16, N'Nerede', 1, 1),
    (@ModuleId, @Q16, N'Ne', 0, 2),
    (@ModuleId, @Q16, N'Kim', 0, 3),
    (@ModuleId, @Q16, N'Ne zaman', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Hava durumunu tanımlayan tüm kelimeleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Hava durumunu tanımlayan tüm kelimeleri seçin.', @TurkishId, @A2, @MultipleChoice, @Vocabulary, 2, 1);

    DECLARE @Q17 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q17, N'güneşli', 1, 1),
    (@ModuleId, @Q17, N'yağmurlu', 1, 2),
    (@ModuleId, @Q17, N'aç', 0, 3),
    (@ModuleId, @Q17, N'rüzgarlı', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: "Kız kardeş" kelimesi İngilizce''de "brother" demektir.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: "Kız kardeş" kelimesi İngilizce''de "brother" demektir.', @TurkishId, @A2, @TrueFalse, @Vocabulary, 2, 1);

    DECLARE @Q18 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q18, N'Doğru', 0, 1),
    (@ModuleId, @Q18, N'Yanlış', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru biçimi seçin: Dün futbol ___ .')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru biçimi seçin: Dün futbol ___ .', @TurkishId, @A2, @SingleChoice, @Practice, 2, 1);

    DECLARE @Q19 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q19, N'oynadık', 1, 1),
    (@ModuleId, @Q19, N'oynuyoruz', 0, 2),
    (@ModuleId, @Q19, N'oynayacağız', 0, 3),
    (@ModuleId, @Q19, N'oynardık', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @A2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'A2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "I like swimming."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "I like swimming."', @TurkishId, @A2, @Translation, @Practice, 2, 1);

    DECLARE @Q20 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q20, N'Yüzmeyi seviyorum', 1, 1),
    (@ModuleId, @Q20, N'Yüzmekten hoşlanıyorum', 1, 2);
END
GO


/* ---------------------------------------------
   B1 questions - 10 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru seçeneği seçin: Daha fazla zamanım ___ daha çok seyahat ederdim.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru seçeneği seçin: Daha fazla zamanım ___ daha çok seyahat ederdim.', @TurkishId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q21 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q21, N'olsaydı', 1, 1),
    (@ModuleId, @Q21, N'olursa', 0, 2),
    (@ModuleId, @Q21, N'olacak', 0, 3),
    (@ModuleId, @Q21, N'oluyor', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'İş ile ilgili tüm kelimeleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'İş ile ilgili tüm kelimeleri seçin.', @TurkishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q22 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q22, N'çalışan', 1, 1),
    (@ModuleId, @Q22, N'maaş', 1, 2),
    (@ModuleId, @Q22, N'tatil', 0, 3),
    (@ModuleId, @Q22, N'ofis', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: "Burada beş yıldır yaşıyorum" cümlesi konuşmacının hâlâ burada yaşadığını ifade eder.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: "Burada beş yıldır yaşıyorum" cümlesi konuşmacının hâlâ burada yaşadığını ifade eder.', @TurkishId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q23 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q23, N'Doğru', 1, 1),
    (@ModuleId, @Q23, N'Yanlış', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "I think this is a good idea."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "I think this is a good idea."', @TurkishId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q24 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q24, N'Bence bu iyi bir fikir', 1, 1),
    (@ModuleId, @Q24, N'Bana göre bu iyi bir fikir', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Son tatiliniz hakkında bir cümle yazın.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Son tatiliniz hakkında bir cümle yazın.', @TurkishId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q25 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q25, N'gittim', 1, 1),
    (@ModuleId, @Q25, N'ziyaret ettim', 1, 2),
    (@ModuleId, @Q25, N'kaldım', 1, 3);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru seçeneği seçin: O geldiğinde biz bir saattir ___ .')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru seçeneği seçin: O geldiğinde biz bir saattir ___ .', @TurkishId, @B1, @SingleChoice, @Practice, 3, 1);

    DECLARE @Q26 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q26, N'bekliyorduk', 1, 1),
    (@ModuleId, @Q26, N'bekliyoruz', 0, 2),
    (@ModuleId, @Q26, N'bekleyeceğiz', 0, 3),
    (@ModuleId, @Q26, N'bekledik', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Yapmak" fiilinin ardından gelebilecek tüm kelimeleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Yapmak" fiilinin ardından gelebilecek tüm kelimeleri seçin.', @TurkishId, @B1, @MultipleChoice, @Vocabulary, 3, 1);

    DECLARE @Q27 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q27, N'egzersiz', 1, 1),
    (@ModuleId, @Q27, N'hata', 1, 2),
    (@ModuleId, @Q27, N'seyahat', 0, 3),
    (@ModuleId, @Q27, N'alışveriş', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @TrueFalse INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'TrueFalse');
DECLARE @Reading INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Reading');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Doğru mu yanlış mı: Türkçede geniş zaman genel doğruları ve alışkanlıkları ifade etmek için kullanılır.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Doğru mu yanlış mı: Türkçede geniş zaman genel doğruları ve alışkanlıkları ifade etmek için kullanılır.', @TurkishId, @B1, @TrueFalse, @Reading, 3, 1);

    DECLARE @Q28 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q28, N'Doğru', 1, 1),
    (@ModuleId, @Q28, N'Yanlış', 0, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "I have been learning Turkish for a long time."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "I have been learning Turkish for a long time."', @TurkishId, @B1, @Translation, @Practice, 3, 1);

    DECLARE @Q29 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q29, N'Uzun zamandır Türkçe öğreniyorum', 1, 1),
    (@ModuleId, @Q29, N'Uzun süredir Türkçe çalışıyorum', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Piyangoyu kazansaydınız ne yapardınız? Bir cümle yazın.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Piyangoyu kazansaydınız ne yapardınız? Bir cümle yazın.', @TurkishId, @B1, @Essay, @Practice, 3, 1);

    DECLARE @Q30 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q30, N'alırdım', 1, 1),
    (@ModuleId, @Q30, N'seyahat ederdim', 1, 2),
    (@ModuleId, @Q30, N'verirdim', 1, 3);
END
GO


/* ---------------------------------------------
   B2 questions - 6 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Önemli" kelimesinin en iyi eş anlamlısını seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Önemli" kelimesinin en iyi eş anlamlısını seçin.', @TurkishId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q31 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q31, N'kritik', 1, 1),
    (@ModuleId, @Q31, N'sıradan', 0, 2),
    (@ModuleId, @Q31, N'küçük', 0, 3),
    (@ModuleId, @Q31, N'belirsiz', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Tüm resmi ifadeleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Tüm resmi ifadeleri seçin.', @TurkishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q32 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q32, N'Talepte bulunmak isterim...', 1, 1),
    (@ModuleId, @Q32, N'Lütfen yapabilir misiniz...', 1, 2),
    (@ModuleId, @Q32, N'Hemen yapacağım', 0, 3),
    (@ModuleId, @Q32, N'Naber kanka', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "Although I was tired, I finished the work."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "Although I was tired, I finished the work."', @TurkishId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q33 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q33, N'Yorgun olmama rağmen işi bitirdim', 1, 1),
    (@ModuleId, @Q33, N'Yorgun olsam da işi tamamladım', 1, 2);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Muğlak" kelimesinin en iyi eş anlamlısını seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Muğlak" kelimesinin en iyi eş anlamlısını seçin.', @TurkishId, @B2, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q34 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q34, N'belirsiz', 1, 1),
    (@ModuleId, @Q34, N'kesin', 0, 2),
    (@ModuleId, @Q34, N'basit', 0, 3),
    (@ModuleId, @Q34, N'neşeli', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @MultipleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'MultipleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Edilgen yapıyı doğru kullanan tüm cümleleri seçin.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Edilgen yapıyı doğru kullanan tüm cümleleri seçin.', @TurkishId, @B2, @MultipleChoice, @Vocabulary, 4, 1);

    DECLARE @Q35 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q35, N'Rapor Sara tarafından yazıldı.', 1, 1),
    (@ModuleId, @Q35, N'Pasta yenildi.', 1, 2),
    (@ModuleId, @Q35, N'Onlar köprüyü inşa etti.', 0, 3),
    (@ModuleId, @Q35, N'Cam top tarafından kırıldı.', 1, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @B2 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'B2');
DECLARE @Translation INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Translation');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Türkçeye çevirin: "I wish I had more time to study."')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Türkçeye çevirin: "I wish I had more time to study."', @TurkishId, @B2, @Translation, @Practice, 4, 1);

    DECLARE @Q36 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q36, N'Keşke çalışmak için daha fazla zamanım olsaydı', 1, 1),
    (@ModuleId, @Q36, N'Daha fazla çalışma zamanım olsaydı keşke', 1, 2);
END
GO


/* ---------------------------------------------
   C1 questions - 4 db
   --------------------------------------------- */

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'Cümleyi en iyi tamamlayan kelimeyi seçin: Argümanı ikna ediciydi; ancak biraz ___ .')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'Cümleyi en iyi tamamlayan kelimeyi seçin: Argümanı ikna ediciydi; ancak biraz ___ .', @TurkishId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q37 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q37, N'kusurlu', 1, 1),
    (@ModuleId, @Q37, N'neşeli', 0, 2),
    (@ModuleId, @Q37, N'sessiz', 0, 3),
    (@ModuleId, @Q37, N'ahşap', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Bununla birlikte" kelimesini kullanarak karmaşık bir cümle yazın.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Bununla birlikte" kelimesini kullanarak karmaşık bir cümle yazın.', @TurkishId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q38 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q38, N'bununla birlikte', 1, 1);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @SingleChoice INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'SingleChoice');
DECLARE @Vocabulary INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Vocabulary');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'En uygun kelimeyi seçin: Yeni politika halk tarafından ___ bir muhalefet ile karşılandı.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'En uygun kelimeyi seçin: Yeni politika halk tarafından ___ bir muhalefet ile karşılandı.', @TurkishId, @C1, @SingleChoice, @Vocabulary, 4, 1);

    DECLARE @Q39 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q39, N'şiddetli', 1, 1),
    (@ModuleId, @Q39, N'neşeli', 0, 2),
    (@ModuleId, @Q39, N'dar', 0, 3),
    (@ModuleId, @Q39, N'sessiz', 0, 4);
END
GO

DECLARE @ModuleId INT = 0;
DECLARE @TurkishId INT = (SELECT LanguageId FROM {databaseOwner}[{objectQualifier}lm_languages] WHERE Code = N'tr');
DECLARE @C1 INT = (SELECT QuestionLevelId FROM {databaseOwner}[{objectQualifier}lm_question_levels] WHERE LevelName = N'C1');
DECLARE @Essay INT = (SELECT QuestionTypeId FROM {databaseOwner}[{objectQualifier}lm_question_types] WHERE TypeName = N'Essay');
DECLARE @Practice INT = (SELECT SkillTypeId FROM {databaseOwner}[{objectQualifier}lm_skill_types] WHERE SkillName = N'Practice');

IF NOT EXISTS (SELECT 1 FROM {databaseOwner}[{objectQualifier}lm_questions] WHERE ModuleId = @ModuleId AND QuestionText = N'"Buna karşın" kelimesini kullanarak karmaşık bir cümle yazın.')
BEGIN
    INSERT INTO {databaseOwner}[{objectQualifier}lm_questions]
    (ModuleId, QuestionText, LanguageId, QuestionLevelId, QuestionTypeId, SkillTypeId, Points, IsActive)
    VALUES
    (@ModuleId, N'"Buna karşın" kelimesini kullanarak karmaşık bir cümle yazın.', @TurkishId, @C1, @Essay, @Practice, 4, 1);

    DECLARE @Q40 INT = SCOPE_IDENTITY();

    INSERT INTO {databaseOwner}[{objectQualifier}lm_answers] (ModuleId, QuestionId, AnswerText, IsCorrect, AnswerOrder)
    VALUES
    (@ModuleId, @Q40, N'buna karşın', 1, 1);
END
GO